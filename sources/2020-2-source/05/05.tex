

\noindent{\small UDC 517.956.47  \hfill {\footnotesize Вестник~СПбГУ.~Прикладная~математика.~Информатика...~\issueyear.~Т.~16.~Вып.~\issuenum}\\
MSC 74G55

}

\vskip2mm

\noindent{\bf Uniqueness solution to the inverse spectral problem\\ with distributed parameters on the graph-star$^{*}$%

 }

\vskip2.5mm

\noindent{\it  A.~P.~Zhabko$\,^1$%
, K.~B.~Nurtazina$\,^2$%
, V.~V.~Provotorov$\,^3$%
%, I.~О. Famylia%$\,^2$%

}

\efootnote{
%%
\vspace{-3mm}\parindent=7mm
%%
\vskip 0.1mm $^{*}$ This work was financially supported by the
Ministry of Education and Science of the Republic Kazakhstan
(project N AP05136197).\par
%%
%%\vskip 2.0mm
%%
\indent{\copyright} Санкт-Петербургский государственный
университет, \issueyear%
}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\thispagestyle{empty} %очищаем стиль страницы
\thispagestyle{fancy} %включаем пользовательский стиль
\renewcommand{\headrulewidth}{0pt}%
\fancyhead[LO]{}%
\fancyhead[RE]{}%
\fancyfoot[LO]{{\footnotesize\rm{\doivyp/spbu10.\issueyear.\issuenum05 } }\hfill\thepage}%
\fancyfoot[RE]{\thepage\hfill{\footnotesize\rm{\doivyp/spbu10.\issueyear.\issuenum05}}}%
% для оформления нижнего колонтитула
\cfoot{} %

\vskip2mm

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

{\footnotesize

\noindent%
$^1$~%
St.\,Petersburg State University, 7--9, Universitetskaya nab.,
St.\,Petersburg,

\noindent%
\hskip2.45mm%
199034, Russian Federation

\noindent%
$^2$~%
N.~Gumilyov Eurasian National University, 2, ul. Satpaeva, Nur-Sultan,

\noindent%
\hskip2.45mm%
010008,~Republic~Kazakhstan

\noindent%
$^3$~%
Voronezh~State~University,~1,~Universitetskaya~pl.,~Voronezh,

\noindent%
\hskip2.45mm%
394006, Russian~Federation


}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\vskip3mm

{\small \noindent \textbf{For citation:} Zhabko~A.~P.,~Nurtazina~K.~B.,~Provotorov~V.~V. Uniqueness solution to the inverse spectral problem with distributed parameters on the graph-star. {\it Vestnik of Saint~Petersburg
University. Applied Mathematics. Computer Science. Control
Processes}, \issueyear, vol.~16, iss.~\issuenum,
pp.~\pageref{p5}--\pageref{p5e}.\\
\doivyp/\enskip%
\!\!\!spbu10.\issueyear.\issuenum05

\vskip3mm

{\leftskip=7mm\noindent In the space of piecewise smooth functions
on a star graph,  the question of the uniqueness  of the recovery
of the differential operator of a boundary value problem from its
spectral characteristics is analyzed. The uniqueness of the
recovery of the coefficient in a differential expression and the
constant in the boundary conditions of a boundary value problem
from spectral data is considered~--- a set of eigenvalues and a
set of norms of the operator's eigenfunctions. The operator of the
boundary value problem has a singularity generated by the
structure of the graph: differential expression is defined on the
interior parts of all the edges of the graph, and in the interior
node of the graph, where the differential expression loses its
meaning, there is a generalized condition of Kirchhoff~--- the
condition of agreement (the condition of conjugating). A spectral
approach is used, which is based on the spectral properties of the
elliptical operator:  the analyticity of Green's function of the
boundary value problem on the spectral parameter, spectral
completeness and the basis property of the set of its
eigenfunctions in the space of square integrable function. The
results are the basis for solving inverse problems for
evolutionary differential systems of parabolic and hyperbolic
types with distributed parameters on the network (graph), the
elliptical part of which contains coefficients to be determined.
In problems of an applied nature, these are, first of all,
parameters that characterize the properties of the transfer of the
solid environment and describe the elastic properties of the
process of deformation of the environment. The approach mentioned
above can also be applied to problems whose space variable is
vector-like and varies in a network-like domain.\\[1mm]
\textit{Keywords}: graph, differential operator, spectral characteristics, inverse spectral problem, uniqueness solution.

}

}

\vskip 4mm

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


\textbf{Introduction.} When studying the questions of the
identification of heat-physical or elastic characteristics of
network-like industrial objects, one of the most important problem
is to identify unknown coefficients in the evolutionary
differential equation, describing the process of heat transfer or
wave process in the  considered  constructions [1--6]. Within the
mathematical models of these processes, this problem is drawn up
in the form of a inverse problem of determining the coefficients
of the evolutionary differential equation along known data on the
observed states of the process or other a priori information (data
of transmitters fixing  the states of the process or some of its
manifestations). In case, where the differential equation
coefficients depend only on the space variable, we naturally come
to analyze the inverse spectral problem of the Sturm---Liouville
type, based on the spectral properties of the elliptical operator
for the evolutionary equation with the distributed parameters on
the graph [2, 4, 7, 8]. In this case, the input information for
the inverse problem is spectral data: a set of eigenvalues and a
set of norms of eigenfunctions.

Below are the conditions of the uniqueness of the inverse spectral
problem of the Sturm---Liouville type with distributed parameters
on the graph-star and recommendations  given for the case of
analysis of the inverse spectral problem on the graph-tree.





\textbf{Setting of a problem, the basic statement.} We adhere to
the concepts and designations accepted in the works [9--12], and
as well use statement of work [9, 11, 12], touching upon the
spectral properties of a elliptical operator with distributed
parameters on a graph-star.

We denote by  $\Gamma$ a geometric graph-star with ribs $\gamma _k \,\,(k=\overline{1,m})$ and a node $\xi$. Each edge $\gamma_k\,\,(k=\overline{1,m-1})$ is parameterized by a section $[0,\pi/2]$  and edge $\gamma_m$ is parameterized by a section $[ \pi/2,\pi]$, in addition, the orientation on the edges is set by these segments.


The spectral problem for functions $y(x)\in C(\Gamma)\cap C^2[\Gamma]$ is determined by a set of equations
\begin{equation}\label{eq1}
- {y}'' + q( x)y = \lambda y,~~\,\,x\in \gamma
_k\,(k=\overline{1,m}),
\end{equation}
on the edges $\gamma _k$ with fixed parametrization, equation
\begin{equation}
\label{eq2} -y'(\pi/2)_{\gamma _m}+\sum\limits_{k = 1}^{m-1}
y'(\pi/2)_{\gamma _k } =0
\end{equation}
in the node $\xi$ and the boundary conditions
\begin{equation}\label{eq3}
{{\begin{array}{*{20}c}{{y}'(0)_{\gamma _k }- hy(0)_{\gamma _k } =
0\,\,(k = \overline{1,m-1}),~~\,\,{y}'(\pi)_{\gamma_m} +
Hy(\pi)_{\gamma_m} = 0 .}\hfill\\
\end{array} }}
\end{equation}
Where $\lambda$ is the spectral parameter, $q(x)$, $h$, $H (H>0)$
are real, $q( x ) \in C(\Gamma)$ and
$q(x)_{\gamma_1}=q(x)_{\gamma_2}=...=q(x)_{\gamma_{m-1}}$ (this
representation of the function $q( x )$  essentially simplifies
the technical calculations and does not affect  essentially  on
the study);  the symbol $\theta(x)_\gamma$ meant  a narrowing of
the function $\theta(x)$ on the edge $\gamma$. Ratios (1), (2)
call the equation on the graph~$\Gamma$.

In the future, we will be necessary the equation that take place for functions  $z(x)\in C^1[0,\pi]\cap C^2(0,\pi)$:
\begin{equation}\label{eq4}
 - z'' + Q( x )z = \lambda z,
\end{equation}
where
\begin{equation}\label{eq5}
{{\begin{array}{*{20}c}
  Q(x)=q(x)_{\gamma_1},\,\,x\in[0,\pi/2),\\
  Q(x)=q(x)_{\gamma_m},\,\,x\in[\pi/2,\pi].\hfill\\
\end{array} }}
\end{equation}


The solution of the equation (4) will be called any function
$z(x)\in C^1[0,\pi]\cap C^2(0,\pi)$ that satisfies the ratio
(4),~(5).

Here's the following statement [9].


\textbf{Theorem 1.} \emph{For any number $z_0$, $z'_0$ the equation} (4) \emph{has the only solution $z( x,\lambda)\in C^1[0,\pi]\cap C^2(0,\pi)$ satisfying  the conditions $z(0,\lambda)=z_0,\,z'(0,\lambda)=z'_0$, which for each fixed $x \in[0,\pi]$ is a entire analytical function by $\lambda$.}



\textbf{Definition.} \emph{The reverse problem for the system} (1)--(3) \emph{is to restore potential $q(x)$ and coefficients $h$, $H$ according to some information about the spectral data of the boundary-value problem} (1)--(3).

The concept of \emph{spectral data of the boundary-value problem} (1)--(3) includes the set of its eigenvalues and norms of eigenfunctions of this problem.





Let functions $\mu(x,\lambda),\,\eta(x,\lambda)\in C^1[0,\pi]\cap
C^2(0,\pi)$ are equation solutions (4) that satisfy the initial conditions
$\mu \left(\pi /2,\lambda \right) = 1$, $\mu'(\pi/2,\lambda)=0$, $\eta(\pi/2,\lambda)= 0$,
$\eta'(\pi /2,\lambda)=1$,
and functions $u(x,\lambda)$, $v(x,\lambda)\in C^1[0,\pi]\cap C^2(0,\pi)$ are the solutions to the equation (4) with initial conditions $u(0,\lambda)=1$, $u'(0,\lambda)=h$, $v(\pi,\lambda)=1$, $v'(\pi,\lambda)=- H$.
By virtue of the theorem 1 with each fixed $\lambda $ function $u\left( {x,\lambda } \right),\,\,v\left( {x,\lambda } \right)$ are a entire analytical function by $\lambda $.
Next, let's look at two systems of function $\{\varphi _k \left( {x,\lambda } \right)\}_1^m$ and
$\{\hat{\varphi }_k \left( {x,\lambda } \right)\}_1^m$:

\begin{equation}\label{eq6}
 \begin{array}{l}
 \mathop {\varphi _k (x,\lambda)}\limits_{(k =
\overline {1,m-1})} =\left\{ {{\begin{array}{*{20}c}
 {u\left( {\pi /2,\lambda } \right)\mu \left( {x,\lambda }
\right),\,\,\,\,x \in\gamma_i\,\,\,\,(i =\overline{1,m -
1},\,i\neq k),} \hfill \\
 {u(x,\lambda),\,\,\,\,x \in \gamma _k,
 \,\,\,\,\,\,\,\,\,} \hfill \\
 {u\left(  {x,\lambda } \right),\,\,\,x \in \gamma_m,
 \,\,\,\,\,\,\,\,\,} \hfill \\
\end{array} }} \right.\\
\varphi _m \left( {x,\lambda } \right) = \left\{
{{\begin{array}{*{20}c}
 {v\left({x,\lambda } \right),\,\,\,\,x \in
\gamma_1,} \hfill \\
 {v\left( {\pi /2,\lambda } \right)\mu \left( {x,\lambda }
\right),\,\,\,\,x \in \gamma_i\,\,\,\,(i = \overline{2,m - 1}
)}, \hfill \\
 {v\left( {x,\lambda } \right),\,\,\,\,x \in
\gamma _m;} \hfill \\
\end{array} }} \right.
 \end{array}
\end{equation}
\begin{equation}\label{eq7}
 \begin{array}{l}
 \hat{\varphi} _1 \left( {x,\lambda } \right) = \left\{
{{\begin{array}{*{20}c}
 {u\left( {x,\lambda }
\right),\,\,\,\,x \in \gamma_i \,\,\,\left( {i = \overline{1,m -
1}}
\right)}, \hfill \\
 {(m-1)u'(\pi/2,\lambda)\eta\left( {x,\lambda } \right)+
 u(\pi/2,\lambda)\mu\left( {x,\lambda } \right),\,\,\,x \in \gamma _m,
 } \hfill \\
\end{array} }} \right.\\
 \mathop {\hat{\varphi} _k \left( {x,\lambda } \right)}\limits_{\left( {k =
\overline {2,m-1} } \right)}  = \left\{ {{\begin{array}{*{20}c}
 {u'\left( {\pi/2,\lambda } \right)\eta \left( {x,\lambda }
\right),\,\,\,\,x \in \gamma_1,} \hfill \\
 {-\delta_{ki}u'(\pi/2,\lambda)\eta\left( {x,\lambda } \right),
 \,\,\,\,x \in \gamma_i\,\,\,(i = \overline{2,m}),} \hfill \\
\end{array} }} \right.\\
\hat{\varphi} _m \left( {x,\lambda } \right) = \left\{
{{\begin{array}{*{20}c}
 {\frac{v'(\pi /2,\lambda)}{m-1}\eta\left( {x,\lambda } \right)+v(\pi/2,\lambda)\mu(x,\lambda),\,\,\,\,x
\in \gamma_i \,\,\,( {i = \overline{1,m - 1}}
)}, \hfill \\
 {v\left( {x,\lambda } \right),\,\,\,\,x \in \gamma _m.
} \hfill \\
\end{array} }} \right. \\
 \end{array}
\end{equation}
Obviously, functions $\varphi _k \left( {x,\lambda }
\right),\,\hat{\varphi} _k \left( {x,\lambda } \right)\,\,\left( {k
= \overline{1,m}} \right)$ are the solutions of the equations (1), (2) and satisfy  the following boundary  conditions:
\[
{{\begin{array}{*{20}c}
  \varphi'_k(0,\lambda)_{\gamma _k }-
h\varphi_k(0,\lambda)_{\gamma _k } = 0\,\,(k =
\overline{1,m-1}),\,\,\,\,\varphi'_m(\pi,\lambda)_{\gamma_m} +
H\varphi_m(\pi,\lambda)_{\gamma_m} = 0,  \hfill \\
\hat{\varphi}'_1(0,\lambda)_{\gamma _1 }-
h\hat{\varphi}_1(0,\lambda)_{\gamma _1 } = 0,  \,\,\,\,
\hat{\varphi}'_k(\pi,\lambda)_{\gamma _k }+
H\hat{\varphi}_k(\pi,\lambda)_{\gamma _k } = 0\,\,(k =
\overline{2,m}).  \hfill \\
\end{array} }}
\]




\textbf{Lemma.} \emph{Systems $\{\varphi _k \left( {x,\lambda }
\right)\}_1^m$ and $\{\hat{\varphi }_k \left( {x,\lambda }
\right)\}_1^m$ form linearly independent systems of solutions to
the equations} (1), (2):  \emph{variety of solutions to the
equations }(1), (2) \emph{$n$-dimen\-sional\-ly, any solution to
the equations }(1), (2) \emph{is a linear combination of functions
}(6) \emph{or functions} (7).


The  system of function $\{\hat{\varphi }_k
\left( {x,\lambda } \right)\}_1^m$ is convenient under  describing the system of eigenfunctions, the system $\{\varphi _k \left( {x,\lambda }
\right)\}_1^m$ to construct  the Green's function of  boundary value problem (1)--(3).

Let $ D\left( \lambda
\right)=(m-1)u'(\pi/2,\lambda)v(\pi/2,\lambda)
-u(\pi/2,\lambda)v'(\pi/2,\lambda)$. We denote by $\Omega$ a lot of eigenvalue of the boundary value problem  (1)--(3) and let $\Omega',\,\Omega'',\,\Omega'''$ is the sets of real numbers $\lambda$: $\Omega'=\{\lambda:\,\,D(\lambda)=0,\,u(\pi/2,\lambda)\neq0\}$, $\Omega''=\{\lambda:\,\,D(\lambda)=0,\,u(\pi/2,\lambda)=0\}$, $\Omega'''=\{\lambda:\,\,D(\lambda)\neq0,\,u(\pi/2,\lambda)=0\}$.



\textbf{Theorem 2.} \textit{Eigenvalues and eigenfunctions of  the boundary value problem}  (1)--(3) \textit{are real. Eigenfunctions that correspond to different eigenvalues are orthogonal in} $L_2\left( \Gamma \right).$ \textit{There is a ratio}
\[
\Omega=\Omega'\cup\Omega''\cup\Omega'''
\]
(\textit{$\Omega',\,\Omega'',\,\Omega'''$ do not have common
elements}), \textit{in addition}: 1) \textit{if eigenvalue
$\lambda_n=\lambda'_n\in\Omega'$, then it is simple}; 2)
\textit{if $\lambda_n=\lambda''_n\in\Omega''$, his multiplicity is
equal} $m-1$; 3) \textit{if $\lambda_n=\lambda'''_n\in\Omega'''$,
his multiplicity is} $m-2$.

The formulated theorem is a special case of the theorem 4 of the work [9].


Beginning which the statements of theorem 2, we will assume: $\left\{
{\lambda _n } \right\}_{n \ge 0} $ if sequence of eigenvalue of the boundary value problem (1)--(3), $\left\{ {\varphi \left({x,\lambda _n } \right)} \right\}_{n \ge 0} $  its corresponding orthogonal system of eigenfunctions.
In addition, the eigenvalues are ordered by the increase of their modules and every  eigenvalue in the sequence $\left\{ {\lambda _n }\right\}_{n \ge 0} $ is repeated as many times as it corresponds to the linearly independent eigenfunctions.



The asymptomatic behavior of the functions that define the
linearly independent systems of solutions of the equations (1),
(2) in a complex $\lambda$-plane of spectral parameter, is set by
analogy with the reasoning of the work [9, theorem 6]. Let
$\lambda = \rho ^2$,\,\,$\tau=\rm Im\,\rho$ and $G_\delta =
\left\{ \rho :\,\,|\rho - k| \ge \delta > 0,\,\,k = 0, 1, 2,...
\right\} $.


\textbf{Theorem 3.}  \emph{The following asymptomatic formulas are correct for $\left| \rho \right| \to +\infty $}:
\[
{{\begin{array}{*{20}c}
\begin{array}{l}
\mu(x,\lambda)=
 {\cos\rho(\frac{\pi}{2}-x)+ {\rm O}\left( \frac{\exp(|\tau|x)}{|\rho|} \right),} \hfill
 \\
\mu'(x,\lambda)=
 {\rho\sin\rho(\frac{\pi}{2}-x)+ {\rm O}\left( \exp(|\tau|x) \right),} \hfill
 \\
\end{array}  \hfill\\
\end{array} }}
\]
\[
{{\begin{array}{*{20}c}
\begin{array}{l}
 {\eta(x,\lambda)=-\frac{1}{\rho}\sin\rho(\frac{\pi}{2}-x)+
 {\rm O}\left( \frac{\exp(|\tau|x)}{|\rho|^2} \right),} \hfill
 \\
 {\eta'(x,\lambda)=\cos\rho(\frac{\pi}{2}-x)+
 {\rm O}\left( \frac{\exp(|\tau|x)}{|\rho|} \right),} \hfill
 \\
\end{array}  \hfill\\
\end{array} }}
\]
\[
{{\begin{array}{*{20}c}
\begin{array}{l}
 {u(x,\lambda)=\cos\rho x+ {\rm O}\left( \frac{\exp(|\tau|x)}{|\rho|} \right),} \hfill
 \\
{u'(x,\lambda)=
 -\rho\sin\rho x+ {\rm O}\left( \exp(|\tau|x) \right),} \hfill
 \\
\end{array}  \hfill\\
\\
\begin{array}{l}
 {v(x,\lambda)=\cos\rho(\pi-x)+
 {\rm O}\left( \frac{\exp(|\tau|x)}{|\rho|} \right),}\hfill
 \\
 {v'(x,\lambda)=
 \rho\sin\rho(\pi-x) + {\rm O}\left( \exp(|\tau|x) \right),} \hfill
 \\
\end{array}  \hfill\\
\end{array} }}
\]
\[
{{\begin{array}{*{20}c}
  \mathop {\varphi _k \left( {x,\lambda }
\right)}\limits_{\left( {k = \overline {1,m-1} } \right)} = \left\{
{{\begin{array}{*{20}c}
 {\cos \rho \frac{\pi}{2}\cos \rho \left( {\frac{\pi}{2} - x}
\right),\mathop {x \in \gamma _i,}\limits_{(i = \overline {1,m -
1},\,i \ne k)}} \hfill
\\
 {\cos \rho x,\,\,\,x \in \gamma_k , } \hfill \\
 {\cos \rho x,\,\,\,x \in \gamma_m, } \hfill \\
\end{array} }} \right.  \,\,+ {\rm O}\left( \frac{\exp(|\tau|x)}{|\rho|} \right),}  \hfill\\
\end{array} }}
\]
\[
{{\begin{array}{*{20}c}\\
\varphi _m \left( {x,\lambda } \right) = \left\{
{{\begin{array}{*{20}c} {\cos \rho(\pi-x),\,\,\,\,x \in\gamma_1,
  } \hfill \\
 {\cos \rho \frac{\pi}{2}\cos \rho (\frac{\pi}{2}- x),\,\,\,
 \mathop {x \in \gamma_i,}\limits_{(i = \overline {2,m - 1})}  } \hfill \\
 {\cos \rho \left( {\pi - x} \right),\,\,\,x \in\gamma _m, } \hfill \\
\end{array} \,\,+{\rm O}\left( \frac{\exp(|\tau|x)}{|\rho|} \right),}} \right.  \hfill\\
\end{array} }}
\]
\[
{{\begin{array}{*{20}c}
\mathop {\varphi '_k \left( {x,\lambda } \right)}\limits_{\left( {k
= \overline {1,m-1} } \right)} = \left\{ {{\begin{array}{*{20}c}
 {\rho\cos \rho \frac{\pi}{2}\sin \rho(\frac{\pi}{2}-x
),\mathop {x \in \gamma _i,}\limits_{(i = \overline {1,m - 1},\,i
\ne k)}} \hfill
\\
 {-\rho\sin \rho x,\,\,\,x \in \gamma_k, } \hfill \\
 {-\rho\sin \rho x,\,\,\,x \in \gamma_m, } \hfill \\
\end{array} \,\,+{\rm O}\left( \exp(|\tau|x) \right),}} \right.  \hfill\\
\\
\varphi' _m \left( {x,\lambda } \right) = \left\{
{{\begin{array}{*{20}c} {\rho\sin \rho \left( {\pi - x} \right),\,\,\,\,x
\in\gamma_1,} \hfill \\
 {\rho\cos \rho \frac{\pi}{2}\sin\rho (\frac{\pi}{2}- x),\,\,
 \mathop {x \in \gamma_i,}\limits_{(i = \overline {2,m - 1})}  } \hfill \\
 {\rho\sin \rho \left( {\pi - x} \right),\,\,\,x \in\gamma _m, } \hfill \\
\end{array} \,\,+{\rm O}\left( \exp(|\tau|x) \right),}} \right.  \hfill\\
\\
\Delta_k \left( \lambda \right) = \rho \sin \rho \pi \left\{
{{\begin{array}{*{20}c}
 {{1}/{2},\,\,\,\,k = \overline {2,m -1} }, \hfill \\
 {\,\,\,1,\,\,\,\,k=1,m}, \hfill \\
\end{array} }} \right.\,\,+{\rm O}\left( \exp(|\tau|x) \right),\,\,\rho \in
G_\delta,   \hfill\\
\\
D(\lambda)= -\frac{1}{2}m\rho\sin \rho\pi  \,\,+{\rm O}\left( \exp(|\tau|x) \right),\,\,\rho \in
G_\delta.   \hfill\\
\end{array} }}
\]



Next, the set  $\Omega$ can be written  in the form $\Omega=\Omega_1\cup\Omega_2$,
$\Omega_1=\Omega'\cup\Omega''$, $\Omega_2=\Omega''\cup\Omega'''$.
Every from set $\Omega_\ell$ contain a infinite set of eigenvalues $\{\lambda^\ell_n\}_{n\geq 0}$, $\,\ell=1,2$.




\textbf{Theorem 4.} \textit{ The function $D(\lambda)$ is uniquely defined by a set of eigenvalues
$\{\lambda^1_n\}_{n\geq0}$ ($\lambda^1_n\in\Omega_1$)}:
\begin{equation}\label{eq11}
D(\lambda)=\frac{m\pi}{2}(\lambda^1_0-\lambda)\prod\limits
_{n=1}^\infty \frac{\lambda^1_n-\lambda}{n^2}.
\end{equation}

\textit{Proof.} The function $D(\lambda)$ is a entire function by
$\lambda$ of order $\frac{1}{2}$ and, consequently, according to
the Hadamard's theorem  $D(\lambda)$ is uniquely defined by their
zeros $\{\lambda^1_n\}_{n\geq0}$ with  precision to a constant
multiplier:
\[
D(\lambda)=C\prod\limits _{n=1}^\infty\left(1-
\frac{\lambda}{\lambda^1_n}\right)
\]
(case $D(0)=0$ can be avoided by shifting the spectrum of the boundary value problem (1)--(3)).
We'll consider  the function
\[
D^\ast(\lambda)=-\rho \sin\rho\pi=-\lambda\pi\prod\limits
_{n=1}^\infty\left(1- \frac{\lambda}{n^2}\right)\!,
\]
then
\[
\frac{D(\lambda)}{D^\ast(\lambda)}=C\frac{\lambda-\lambda^1_0}{\lambda^1_0\pi\lambda}
\prod\limits_{n=1}^\infty\frac{n^2}{\lambda^1_n}\prod\limits
_{n=1}^\infty\left(1+
\frac{\lambda^1_n-n^2}{n^2-\lambda}\right)\!.
\]
Constant $C$ is determined when passage to the limit in the last ratio at $\lambda\rightarrow -\infty$:
\[
C=\pi\lambda^1_0\frac{m}{2}\prod\limits_{n=1}^\infty\frac{\lambda^1_n}{n^2},
\]
which leads to  (8).



\so{Remark 1}.\ Similarly, it is shown that a function
$u(\frac{\pi}{2},\lambda)$ is uniquely defined by a~set of
eigenvalues $\{\lambda^2_n\}_{n\geq0}$ ($\lambda^2_n\in\Omega_2$).






Let's define on functions $y(x)\in C^2[\Gamma]$ a generalized differential expression of ratios:
\[
{{\begin{array}{*{20}c}
\Lambda y=\left\{
{{\begin{array}{*{20}c}
 {-y''+q(x)y,\quad x\in\gamma_k \,\,\,(k = \overline{1,m}), } \hfill \\
 {\,\,y'(\pi/2)_{\gamma_m}-\sum \limits
_{k=1}^{m-1}y'(\pi/2)_{\gamma_k},\quad x\in \xi\,\,\,(x=\pi/2).} \hfill \\
\end{array} }} \right.  \hfill\\
\end{array} }}
\]





The Green's function of the boundary value problem  (1)--(3) we shall call a function $G\left( {x,t,\lambda } \right)$ such that the solution of a problem $\Lambda y-\lambda y=f(x)$ that meets the boundary  conditions (3), in any function $f(x)$  can be presented in the form
\[
y(x,\lambda) = \int\limits_\Gamma G(x,t,\lambda )f( t )dt.
\]
Let
\[
{{\begin{array}{*{20}c}
 \delta(\lambda) =\langle
u(x,\lambda),v(x,\lambda)\rangle=\left|{{\begin{array}{*{20}c}
 {u(\pi/2,\lambda)\,\,\,\,\,\,v(\pi/2,\lambda)} \hfill \\
 {u'(\pi/2,\lambda)\,\,\,\,\,v'(\pi/2,\lambda)} \hfill \\
\end{array} }} \right|, \hfill\\
\\
\Delta_k (\lambda)= \langle
\varphi_k(x,\lambda),\varphi_m(x,\lambda)\rangle_{\gamma_k}\,\,\,(k=\overline{1,m-1}),\hfill\\
\Delta_m(\lambda)= \langle
\varphi_1(x,\lambda),\varphi_m(x,\lambda)\rangle_{\gamma_m}.\hfill\\
\end{array} }}
\]
Accordingly to with the views (6) functions $\varphi_k(x,\lambda)$
we have
\[
{{\begin{array}{*{20}c} \Delta_1 (\lambda)=\delta(\lambda),\,\,
 \Delta_k(\lambda)=-v(\pi/2,\lambda)u'(\pi/2,\lambda)\,\,(k=\overline{2,m-1})\hfill\\
\end{array} }}
\]
and because the functions $\varphi_1(x,\lambda),\varphi_m(x,\lambda)$ satisfy  the conditions of agreement (2) in the node $\xi$, then
\[
{{\begin{array}{*{20}c}
 \Delta_m(\lambda)=\langle
u(x,\lambda),v(x,\lambda)\rangle_{x=\pi/2}=
\delta(\lambda).\hfill\\
\end{array} }}
\]

Let's $G\left( {x,t,\lambda } \right)$ determine the ratio $G\left(
{x,t,\lambda } \right) = G_0 \left( {x,t,\lambda } \right) + g\left(
{x,\lambda } \right)$, where

\[
{{\begin{array}{*{20}c}
{G_0(x,t,\lambda) =\frac{\delta _{ik} }{
{\Delta_k }( \lambda)}\left\{ {{\begin{array}{*{20}c}
 {\left\{ {{\begin{array}{*{20}c}
 {\varphi _k \left( {x,\lambda } \right)\varphi _m \left( {t,\lambda }
\right),\,\,\,x \le t,} \hfill \\
 {\varphi _k \left( {t,\lambda } \right)\varphi _m \left( {x,\lambda }
\right),\,\,\,t \le x,} \hfill \\
\end{array} }} \right.\,\mathop{x\in \gamma_k}\limits_{(k=\overline{1,m-1})},\,
\mathop{t\in\gamma_i}\limits_{(i=\overline{1,m})}}, \hfill \\
 {\left\{ {{\begin{array}{*{20}c}
 {\varphi _1 \left( {x,\lambda } \right)\varphi _m \left( {t,\lambda }
\right),\,\,\,x \le t,} \hfill \\
 {\varphi _1 \left( {t,\lambda } \right)\varphi _m \left( {x,\lambda }
\right),\,\,\,t \le x,} \hfill \\
\end{array} }} \right.\,\,\,\mathop{x\in \gamma_k}\limits_{(k=m)},\,\,\,
\mathop{t\in\gamma_i}\limits_{(i=\overline{1,m})}}, \hfill \\
\end{array} }} \right.
}\hfill \\
\end{array} }}
\]
\[
{{\begin{array}{*{20}c}
{g(x,\lambda)_{\gamma_k} = c_k \varphi_k( x,\lambda),\,\,k =
\overline{1,m},}\hfill\\
\end{array} }}
\]
and, granting  the continuity of the solution $y(x,\lambda)$ on $\Gamma$ and the ratio (2) in the node $\xi$, $c_k$ $(k = \overline{1,m})$ are defined as the solution of the system of linear algebraic equations

\[
 \left\{ {{\begin{array}{ll}
 u_k\left( {\pi /2,\lambda } \right)c_k - v\left( {\pi
/2,\lambda } \right)c_m \mathop {= }\limits_{( k = \overline
{1,m-1})}  G_0 \left( {x,t,\lambda } \right)_{x = \pi/2 \in
\gamma_m } -
G_0 \left( {x,t,\lambda } \right)_{x =
\pi/2 \in \gamma_k },  \\
 \sum\limits_{k = 1}^{m - 1}{u}'_k\left( {\pi/2,\lambda }
\right)c_k -v'\left( {\pi/2,\lambda } \right)c_m =\frac{\partial
G_0 }{\partial x}\left( {x,t,\lambda } \right)_{x = \pi /2 \in
\gamma_m } - \sum\limits_{k = 1}^{m - 1} {\frac{\partial G_0 }{\partial x}}
\left( {x,t,\lambda } \right)_{x = \pi /2 \in
\gamma_k } .  \\
\end{array} }} \right.
\]
The determinant of system is equals $[u(\pi/2,\lambda)]^{m - 2}D( \lambda)$; coefficients $c_k\equiv c_k(t,\lambda )$, $t\in\Gamma$ $(k =\overline{ 1,m})$, granting the view $\Delta_k(\lambda)$ ($k =\overline{ 1,m}$), are determined by the following ratios:


under $t\in \gamma_1$:
\[
{{\begin{array}{ll}
c_1(t,\lambda)=-\frac{(m-2)v^2(\pi/2,\lambda)u'(\pi/2,\lambda)}
{\Delta_1(\lambda)u(\pi/2,\lambda)D(\lambda)}\varphi_1(t,\lambda),\hfill \\
\end{array} }}
\]
\[
{{\begin{array}{ll}
c_k(t,\lambda)=
-\frac{v(\pi/2,\lambda)}{u(\pi/2,\lambda)D(\lambda)}\varphi_1(t,\lambda)\,\,\,
(k=\overline{2,m-1}),  \hfill \\
\end{array} }}
\]
\[
{{\begin{array}{ll}
 c_m(t,\lambda) =-\frac{1}{D(\lambda)}\varphi_1(t,\lambda),
\end{array} }}
\]

under $t\in \gamma_2$:
\[
{{\begin{array}{ll}
c_k(t,\lambda)=-\frac{v(\pi/2,\lambda)}{u(\pi/2,\lambda)D(\lambda)}\varphi_2(t,\lambda)\,\,\,
(k=\overline{1,m-1},\,\,k\neq2),  \hfill \\
\end{array} }}
\]
\[
{{\begin{array}{ll}
c_2(t,\lambda) =-\frac{v(\pi/2,\lambda)\widehat{D(\lambda)}}
{\Delta_2(\lambda)u(\pi/2,\lambda)D(\lambda)}\varphi_2(t,\lambda),  \hfill \\
\end{array} }}
\]
\[
{{\begin{array}{ll}
 c_m(t,\lambda) =- \frac{1}{D(\lambda)}\varphi_2(t,\lambda),
\end{array} }}
\]

\[
...
\]

under $t\in \gamma_{m-1}$:
\[
{{\begin{array}{ll}
c_k(t,\lambda)=-\frac{v(\pi/2,\lambda)}{u(\pi/2,\lambda)D(\lambda)}\varphi_{m-1}(t,\lambda)\,\,\,
(k=\overline{1,m-1},\,\,k\neq m-2),  \hfill \\
\end{array} }}
\]
\[
{{\begin{array}{ll}
c_{m-2}(t,\lambda)
=-\frac{v(\pi/2,\lambda)\widehat{D(\lambda)}}{\Delta_{m-1}(\lambda)u(\pi/2,\lambda)
D(\lambda)}\varphi_{m-1}(t,\lambda), \hfill \\
\end{array} }}
\]
\[
{{\begin{array}{ll}
c_m(t,\lambda) =- \frac{1}{D(\lambda)}\varphi_{m-1}(t,\lambda),
\end{array} }}
\]

under $t\in \gamma_{m}$:
\[
{{\begin{array}{ll}
c_k(t,\lambda)=-\frac{1}{D(\lambda)}\varphi_m(t,\lambda)\,\,(k=\overline{1,m-1}), \hfill \\
\end{array} }}
\]
\[
{{\begin{array}{ll}
c_m(t,\lambda)
=-\frac{(m-2)u(\pi/2,\lambda)u'(\pi/2,\lambda)}{\Delta_m(\lambda)
D(\lambda)}\varphi_m(t,\lambda), \hfill \\
\end{array} }}
\]
here $\widehat{D(\lambda)}=(m-2)u'(\pi/2,\lambda)v(\pi/2,\lambda)-u(\pi/2,\lambda)v'(\pi/2,\lambda)$.



Thus if $\lambda$ not zero for $[u(\pi/2,\lambda)]^{m - 2}D(
\lambda)$, the Green's function of  the boundary value problem
(1)--(3) takes the form
\begin{equation}\label{eq9}
G\left( {x,t,\lambda } \right) = G_0 \left( {x,t,\lambda } \right) +
c_k \left( {t,\lambda } \right)\varphi _k \left( {x,\lambda }
\right),\,\,\,x \in \gamma _k \,\,\left( {k = \overline {1,m} }
\right).
\end{equation}
The continuity and symmetry of the function $G\left({x,t,\lambda } \right)$ (see (9)) by variables $x,t\in\Gamma$ is obvious.



\textbf{Theorem 5.} \emph{The system of eigenfunctions $\left\{ {\varphi \left({x,\lambda _n } \right)} \right\}_{n \ge 0} $ of the boundary value problem} (1)--(3) \emph{is complete and forms a basis in space $L_2(\Gamma)$.}


Proof of the theorem uses a view of solution of the boundary value
problem (1)--(3) using the Green's function, where the right part
of the equation (1) contain a arbitrary completely continuous
function $f(x)$. In addition uses a contour integral method when
calculating it on extended contours. The full proof of the
statement of the theorem is presented in [9, theorem~7].

Next, get over  to the proof of the main statement --- the
uniqueness of solution of the inverse problem (1)--(3).



Let $L(q(x),h,H)$ is the boundary value problem (1)--(3).  Let's agree that along with the boundary value problem $L(q(x),h,H)$ is consider the problem $L(\tilde{q}(x),\tilde{h},\tilde{H})$ the same form, but with other input data.  If certain symbol $\chi$  means a  object of problem $L(q(x),h,H)$,  then symbol  $\tilde{\chi}$ will denote by a analogous object relating to the problem $L(\tilde{q}(x),\tilde{h},\tilde{H})$. From the theorem 4 follows that $D(\lambda)=\widehat{D(\lambda)}$, $u(\pi/2,\lambda)=\tilde{u}(\pi/2,\lambda)$.
Let, as above, $\{\lambda_n\}_{n\geq 0}$ if a set of eigenvalues of the problem (1)--(3) and $\{\omega_n\}_{n\geq 0}$ it corresponds to the sequence $\varphi(x,\lambda _n)$:  $\omega_n= \int\limits_{\Gamma}\varphi^2(x,\lambda _n)dx$ \,\,($n=0,1,...$).





\textbf{Theorem 6.} {\it The inverse problem} (1)--(3) {\it has
the unique solution: if the spectral characteristics
$\{\lambda_n\}_{n\geq 0}$, $\{\omega_n\}_{n\geq 0}$ of problems
$L(q(x),h,H)$ and $L(\tilde{q}(x),\tilde{h},\tilde{H})$ coincide,
then $L(q(x),h,H)=L(\tilde{q}(x),\tilde{h},\tilde{H})$, i.~e.
$q(x)=\tilde{q}(x)$ almost everywhere on $\Gamma$ and
$h=\tilde{h},\,H=\tilde{H}$. }


\textit{Proof.} The approach used here belongs to N. Levinson. The
approach based on the contour integral method when calculating it
on extended contours. In what follows the method was improved,
thanks first of all to fundamental work [8]. With evidence we rely
on the reasoning of theorems 7 and 8 work [9].




Let $f\left( x\right)$ is arbitrary absolutely continuous on $\Gamma$ function. We consider  the function $Y\left( {x,\lambda } \right)$, $x\in{\Gamma}$, where
\[
Y(x,\lambda )_{\gamma _1 } = \frac{1}{\lambda}\left\{
\frac{1}{\Delta_1(\lambda)} (\varphi _m (x,\lambda )\int
\limits_0^x [ - \tilde{\varphi} ''_1 (t,\lambda) + \tilde{q}( t )
\tilde{\varphi} _1 (t,\lambda)]f(t)dt \right. \,+
\]
\[
+\,\varphi _1(x,\lambda )\int\limits_x^{\pi/2}
[- \tilde{\varphi}''_m(t,\lambda) + \tilde{q}( t ) \tilde{\varphi}
_m ( t,\lambda)]f( t)dt \,-
\]
\[
-\,\frac{(m-2)u'(\pi/2,\lambda)v^2(\pi/2,\lambda)}
{u(\pi/2,\lambda)D(\lambda)} \varphi_1 \left( {x,\lambda }
\right)\int\limits_{\gamma _1 } [ -\tilde{\varphi}''_1(t,\lambda ) +
\tilde{q}( t )\tilde{\varphi} _1 ( t,\lambda )] f( t )dt)\,-
\]
\[
 -\,\frac{v(\pi/2,\lambda)}{u(\pi/2,\lambda)D(\lambda)}\varphi _1 \left( {x,\lambda } \right)
\sum\limits_{k=2}^{m-1}\int\limits_{\gamma _k }{\left[ { -
{\tilde{\varphi} }''_k \left( {t,\lambda } \right) + \tilde{q}\left(
t \right)\tilde{\varphi} _k \left( {t,\lambda } \right)} \right]}
f\left( t \right)dt \, -
\]
\[
-\,\left. \frac{1}{D(\lambda)}\varphi _1(x,\lambda)
\int\limits_{\gamma _m }[-\tilde{\varphi}''_m(t,\lambda) +
\tilde{q}(t)\tilde{\varphi}_m(t,\lambda)] f(t)dt \right\} ,
\]

\[
Y(x,\lambda)_{\gamma _2 } = \frac{1}{\lambda}\left\{
-\frac{v(\pi/2,\lambda)}{u(\pi/2,\lambda)D(\lambda)}\varphi _2(
x,\lambda)\mathop{\sum\limits_{k=1}^{m-1}}\limits_{(k\neq2)}
\int\limits_{\gamma _k }[-\tilde{\varphi}''_k(t,\lambda) +
\tilde{q}(t)\tilde{\varphi} _k(t,\lambda)] f( t)dt \right. \,+
\]
\[
+\,\frac{1}{\Delta_2(\lambda)} (\varphi _m (x,\lambda )\int
\limits_0^x [- \tilde{\varphi} ''_2 (t,\lambda) + \tilde{q}( t )
\tilde{\varphi} _2 (t,\lambda)]f(t)dt \,+
\]
\[
 + \,\varphi _2(x,\lambda )\int\limits_x^{\pi /2}
[- \tilde{\varphi}''_m(t,\lambda) + \tilde{q}( t ) \tilde{\varphi}
_m ( t,\lambda)]f( t)dt \,-
\]
\[
-\,\frac{v(\pi/2,\lambda)\widehat{D(\lambda)}}
{u(\pi/2,\lambda)D(\lambda)} \varphi_2 \left( {x,\lambda }
\right)\int\limits_{\gamma _2 }^{} [ -\tilde{\varphi}''_2(t,\lambda
) + \tilde{q}( t )\tilde{\varphi} _2 ( t,\lambda )] f( t )dt)\,-
\]
\[
 -\, \left. \frac{1}{D(\lambda)}\varphi _2 \left( {x,\lambda } \right)
\int\limits_{\gamma _m }{\left[ { - {\tilde{\varphi} }''_m \left(
{t,\lambda } \right) + \tilde{q}\left( t \right)\tilde{\varphi} _m
\left( {t,\lambda } \right)} \right]} f\left( t \right)dt
\right\};
\]

\[.\,.\,.\]

\[
Y\left(x,\lambda \right)_{\gamma_m }  = \frac{1}{\lambda} \left\{
 -\frac{1}{D(\lambda)}\varphi_m \left( {x,\lambda } \right)\sum\limits_{k=1}^{m-1}\int\limits_{\gamma _k }{\left[ { -
{\tilde{\varphi} }''_k\left( {t,\lambda } \right) +
\tilde{q}\left( t \right)\tilde{\varphi} _k \left( {t,\lambda }
\right)} \right]} f\left( t \right)dt \right. \,+
\]
\[
+\,\frac{1}{\Delta_m(\lambda)} (\varphi _m (x,\lambda )\int
\limits_{\pi/2}^x [ - \tilde{\varphi} ''_1 (t,\lambda) + \tilde{q}(
t ) \tilde{\varphi} _1 (t,\lambda)]f(t)dt \,+
\]
\[
 + \,\varphi _1(x,\lambda )\int\limits_x^\pi
[- \tilde{\varphi}''_m(t,\lambda) + \tilde{q}( t ) \tilde{\varphi}
_m ( t,\lambda)]f( t)dt \,-
\]
\[
-\, \left. \frac{(m-2)u(\pi/2,\lambda)u'(\pi/2,\lambda)}
{D(\lambda)} \varphi_m \left( {x,\lambda }
\right)\int\limits_{\gamma _m }[ -\tilde{\varphi}''_m(t,\lambda )
+ \tilde{q}( t )\tilde{\varphi} _m( t,\lambda )] f( t )dt)
\right\},
\]
where, as above, $Y\left(x,\lambda \right)_{\gamma}$ is a
narrowing on the edge $\gamma$. \pagebreak

Considering the view (6) of functions $\varphi _k \left(x,\lambda
\right)$ in the form of function $Y(x,\lambda)$, $x\in\Gamma$, we
will calculate the integrals by parts containing the second
derivatives from $\tilde{\varphi} _k\left( {x,\lambda } \right)$:
\begin{equation}
\label{eq10} Y\left( {x,\lambda } \right)_{\Gamma} = \frac{f\left(
x \right)}{\lambda } + \frac{1}{\lambda }\left( {S_1 \left(
{x,\lambda } \right) + S_2 \left( {x,\lambda } \right)} \right).
\end{equation}
In (10)
\[
S_1 ( x,\lambda )_{\gamma _1 } = \frac{1}{\Delta_1( \lambda )}
\left\{ \varphi
_m(x,\lambda)\int\limits_0^x\tilde{\varphi}'_1(t,\lambda )f'( t)dt
\right. \,+
\]
\[
+\,\left. \varphi _1(x,\lambda
)\int\limits_x^{\pi/2}\tilde{\varphi}'_m( t,\lambda )f'( t )dt -
 \frac{(m-2)u'(\pi/2,\lambda)v^2(\pi/2,\lambda)}
{u(\pi/2,\lambda)D(\lambda)}\varphi _1( x,\lambda
)\int\limits_{\gamma _1 } \tilde{\varphi}'_1(t,\lambda)f'( t )dt
\right\} \,-
\]
\[
-\,\frac{v(\pi/2,\lambda)}{u(\pi/2,\lambda)D(\lambda)}\varphi _1
\left( {x,\lambda } \right)
\sum\limits_{k=2}^{m-1}\int\limits_{\gamma _k }^{} {{\tilde{\varphi}
}'_k } \left( {t,\lambda } \right){f}'( t )dt-
\frac{1}{D(\lambda)}\varphi _1 \left( {x,\lambda } \right)
\int\limits_{\gamma _m }{{\tilde{\varphi} }'_m } \left( {t,\lambda }
\right){f}'( t )dt,
\]
\[
\mathop{S_1 ( x,\lambda )_{\gamma
_i}}\limits_{(i=\overline{2,m-1})} = \frac{1}{\Delta_i( \lambda )}
\left\{ \varphi
_m(x,\lambda)\int\limits_0^x\tilde{\varphi}'_i(t,\lambda )f'( t)dt
\right. \,+
\]
\[
+\,\left. \tilde{\varphi} _i(x,\lambda
)\int\limits_x^{\pi/2}\tilde{\varphi}'_m( t,\lambda )f'( t )dt -
\frac{v(\pi/2,\lambda)\widehat{D(\lambda)}}
{u(\pi/2,\lambda)D(\lambda)}\varphi _i( x,\lambda
)\int\limits_{\gamma _i }\tilde{\varphi}'_i(t,\lambda)f'( t )dt
\right\} \,-
\]
\[
-\,\frac{v(\pi/2,\lambda)}{u(\pi/2,\lambda)D(\lambda)}\varphi _i
\left( {x,\lambda }
\right)\mathop{\sum\limits_{k=1}^{m-1}}\limits_{(k\neq i)}
\int\limits_{\gamma _k } {{\tilde{\varphi} }'_k } \left( {t,\lambda
} \right){f}'( t )dt  -
\frac{1}{D(\lambda)}\varphi _i \left( {x,\lambda } \right)
\int\limits_{\gamma _m }{{\tilde{\varphi }}'_m} \left( {t,\lambda }
\right){f}'( t )dt,
\]
\[
S_1 ( x,\lambda )_{\gamma_m } = \frac{1}{\Delta_m( \lambda )}
\left\{ \varphi
_m(x,\lambda)\int\limits_{\pi/2}^x\tilde{\varphi}'_1(t,\lambda
)f'( t)dt \right. \,+
\]
\[
+\, \left. \tilde{\varphi} _1(x,\lambda
)\int\limits_x^{\pi}\tilde{\varphi}'_m( t,\lambda )f'( t )dt -
 \frac{(m-2)u(\pi/2,\lambda)u'(\pi/2,\lambda)}
{D(\lambda)}\varphi _m( x,\lambda )\int\limits_{\gamma _m}
\tilde{\varphi}'_m(t,\lambda)f'( t )dt \right\} \,-
\]
\[
-\,\frac{1}{D(\lambda)}\varphi _m \left( {x,\lambda } \right)
\sum\limits_{k=1}^{m-1}\int\limits_{\gamma _k
}\tilde{\varphi}'_k(t,\lambda ){f}'( t )dt ;
\]
the function $S_2(x,\lambda)_\Gamma$ is represented by the
remaining summands $Y(x,\lambda)_\Gamma$ that do not contain
derivatives $\tilde{\varphi}'_k(t,\lambda
)\,\,(k=\overline{1,m})$. The asymptomatic formulas for functions
$\tilde{\varphi}_k(t,\lambda )\,\,(k=\overline{1,m})$ are the same
as those of functions $\varphi_k(t,\lambda
)\,\,(k=\overline{1,m})$ (statements of the theorem 3), therefore
for the function $S_2 (x,\lambda)_{\Gamma}$ take place estimate
\begin{equation}\label{eq11}
\mathop {\max }\limits_{x\in\Gamma} \left| {S_2 \left( {x,\lambda }
\right)} \right| \le \frac{C}{\left| \rho \right|},\,\,\,\,\rho \in
G_\delta ,\,\,\,\,\left| \rho \right| \ge \rho ^ *
\end{equation}
under fixed $\delta> 0$ and large enough $\rho^ *>0$.\pagebreak

We consider  the function $S_1(x,\lambda)_{\Gamma}$. Let  $g\left(
x \right) = {f}'\left( x \right)$, $x \in \Gamma$ (derivatives in
the node $\xi$ one-sided). Suppose at first that $g\left( x
\right)$ it is absolutely continuous on each of the edges $\gamma
_k \,\,\,\left( {k = \overline{1,m}} \right)$. Then integration by
piece reduce $S_1 \left( {x,\lambda } \right)$ to a view
containing only a function $g\left( x\right)$ under the derivative
sign.  Because of the asymptotic formulas by theorem~3 get a
estimate~(11):
\[
\mathop {\max }\limits_{x\in\Gamma} \left| {S_1 \left( {x,\lambda }
\right)} \right| \le \frac{C}{\left| \rho \right|},\,\,\,\,\rho \in
G_\delta ,\,\,\,\,\left| \rho \right| \ge \rho ^ * .
\]

Let now  $g\left( x \right) \in L{\left( \Gamma \right)}.$
We will fix $\varepsilon > 0$ and choose a absolutely continuous function so that
\[
\int\limits_\Gamma \left| g( t ) - g_\varepsilon ( t)
\right|dt<\frac{\varepsilon}{2{C}^0},
\]
where ${C}^0= \mathop {\max }\limits_{x \in \Gamma} \mathop {\sup
}\limits_{\rho \in G_\delta } \left| {S_1 \left( {x,\lambda }
\right)} \right|$. Then at $\rho \in G_\delta ,\,\,\,\,\rho \ge
\rho ^ * $ we have
\[
\mathop {\max }\limits_{x \in \Gamma} \left| {S_1 \left( {x,\lambda
} \right)} \right| \le \mathop {\max }\limits_{x \in \Gamma} \left|
{S_1 \left( {x,\lambda ,q_\varepsilon } \right)} \right| + \mathop
{\max }\limits_{ \Gamma } \left| {S_1 \left( {x,\lambda ;g -
g_\varepsilon } \right)} \right| \le \varepsilon /2 +{C}^0/| \rho |.
\]
Consequently, exist $\rho ^0> 0$ so that $\mathop {\max }\limits_{\Gamma} \left| {S_1\left( {x,\lambda } \right)} \right| \le \varepsilon $ when $\rho>\rho ^0$.  Because of the arbitrariness, we get
\begin{equation}\label{eq12}
\mathop {\lim }\limits_{\left| \rho \right| \to \infty
} \mathop {\max }\limits_{x \in \Gamma } \left| {S_1 \left(
{x,\lambda } \right)} \right| = 0,\,\,\,\,\rho\in G_\delta.
\end{equation}

We consider the contour integral
\begin{equation}\label{eq13}
I_N \left( x \right) = \frac{1}{2\pi i}\int\limits_{R_N }
{Y\left( {x,\lambda } \right)d\lambda } ,
\end{equation}
here $R_N = \left\{ \lambda :\,\,\,\left| \lambda \right| = \left( {N
+ 1/2} \right)^2 \right\}$, $N\geq1$, is the integer (counter-clockwise bypass).
From (12), (13) we get $I_N \left( x \right) = f\left( x \right) + \varepsilon _N \left( x
\right)$,  $\mathop {\lim }\limits_{N \to \infty } \mathop {\max
}\limits_{x\in\Gamma} \left| {\varepsilon _N \left( x \right)}
\right| = 0$.

On the other hand, you can calculate the integral $I_N \left( x \right)$ with the help of theorem of residue:
\[
\mathop {\rm Res
}\limits_{\lambda=\lambda_n}Y(x,\lambda)_{\gamma_1} = \mathop
{\lim }\limits_{\lambda \to \lambda_n }
(\lambda-\lambda_n)\biggl\{\frac{1}{\Delta_1(\lambda)}
\biggl(\varphi _m (x,\lambda )\int \limits_0^x\tilde{\varphi} _1
(t,\lambda)f(t)dt \,+
\]
\[
 + \,\varphi _1(x,\lambda )\int\limits_x^{\pi/2}
 \tilde{\varphi}_m ( t,\lambda)f( t)dt -
\frac{(m-2)u'(\pi/2,\lambda)v^2(\pi/2,\lambda)}
{u(\pi/2,\lambda)D(\lambda)} \varphi_1 \left( {x,\lambda }
\right)\int\limits_{\gamma _1 }\tilde{\varphi} _1 ( t,\lambda ) f(
t )dt \biggr)\,-
\]
\[
 -\,\frac{v(\pi/2,\lambda)}{u(\pi/2,\lambda)D(\lambda)}\varphi _1 \left( {x,\lambda } \right)
\sum\limits_{k=2}^{m-1}\int\limits_{\gamma _k }\tilde{\varphi} _k
\left( {t,\lambda } \right) f\left( t \right)dt  -
\frac{1}{D(\lambda)}\varphi _1 \left( {x,\lambda } \right)
\int\limits_{\gamma _m }\tilde{\varphi} _m \left( {t,\lambda }
\right) f\left( t \right)dt \biggr\},
\]

\[
\mathop {\rm Res
}\limits_{\lambda=\lambda_n}Y(x,\lambda)_{\gamma_2} = \mathop
{\lim }\limits_{\lambda \to \lambda_n }
(\lambda-\lambda_n)\biggl\{
-\frac{v(\pi/2,\lambda)}{u(\pi/2,\lambda)D(\lambda)}\varphi _2(
x,\lambda)\mathop{\sum\limits_{k=1}^{m-1}}\limits_{(k\neq2)}
\int\limits_{\gamma _k } \tilde{\varphi} _k(t,\lambda) f( t)dt \,+
\]
\[
+\,\frac{1}{\Delta_2(\lambda)} \biggl(\varphi _m (x,\lambda )\int
\limits_0^x
 \tilde{\varphi}_2 (t,\lambda)f(t)dt
 + \varphi _2(x,\lambda )\int\limits_x^{\pi /2}
 \tilde{\varphi}_m ( t,\lambda)f( t)dt \,-
\]
\[
-\,\frac{v(\pi/2,\lambda)\widehat{D}(\lambda)}
{u(\pi/2,\lambda)D(\lambda)} \varphi_2 \left( {x,\lambda }
\right)\int\limits_{\gamma _2 }\tilde{\varphi} _2 ( t,\lambda ) f(
t )dt \biggr)- \frac{1}{D(\lambda)}\varphi _2 \left( {x,\lambda }
\right) \int\limits_{\gamma _m }\tilde{\varphi} _m \left(
{t,\lambda } \right) f\left( t \right)dt \biggr\},
\]
\[.\,.\,.\]
\[
\mathop {\rm Res
}\limits_{\lambda=\lambda_n}Y(x,\lambda)_{\gamma_m}  = \mathop
{\lim }\limits_{\lambda \to \lambda_n }
(\lambda-\lambda_n)\biggl\{
 -\frac{1}{D(\lambda)}\varphi_m \left( {x,\lambda } \right)
\sum\limits_{k=1}^{m-1}\int\limits_{\gamma _k }\tilde{\varphi} _k
\left( {t,\lambda } \right) f\left( t \right)dt \,+
\]
\[
+\,\frac{1}{\Delta_m(\lambda)} \biggl(\varphi _m (x,\lambda )\int
\limits_{\pi/2}^x \tilde{\varphi} _1 (t,\lambda)f(t)dt
 + \varphi _1(x,\lambda )\int\limits_x^\pi
\tilde{\varphi} _m ( t,\lambda)f( t)dt \,-
\]
\[
-\,\frac{(m-2)u(\pi/2,\lambda)u'(\pi/2,\lambda)} {D(\lambda)}
\varphi_m \left( {x,\lambda } \right)\int\limits_{\gamma _m
}\tilde{\varphi} _m( t,\lambda ) f( t )dt \biggr)\biggr\}.
\]

From the conditions of the theorem, as well taking into account the statements of the theorem 5 and using the technique of transforms of expressions $\mathop {\rm Res}\limits_{\lambda=\lambda_n}Y(x,\lambda)_{\gamma_k}$, $k=\overline{1,m}$  (see proof of the theorem 8 of work [9]), we get

\[
I_N \left( x
\right)=\sum\limits_{n = 1}^N \frac{1}{\alpha_n}\varphi(
x,\lambda_n)\int\limits_\Gamma {\tilde{\varphi} \left( {t,{\lambda
}_n } \right)} f\left( t \right)dt,
\]
it means

\[
f(x)=\sum\limits_{n = 1}^\infty \frac{1}{\alpha_n}\varphi(
x,\lambda_n)\int\limits_\Gamma {\tilde{\varphi} \left( {t,{\lambda
}_n } \right)} f\left( t \right)dt,
\]
here $\alpha_n=\int\limits_\Gamma \varphi^2(t,\lambda_n)dt$, $\lambda_n\in\Omega$,
$\varphi( x,\lambda_n)$ is  eigenfunction of the  problem $L(q(x),h,H)$, $\tilde{\varphi} \left( {t,{\lambda}_n } \right)$ is eigenfunction of the problem $L(\tilde{q}(x),\tilde{h},\tilde{H})$. Together with basis property of $\{\varphi(x,\lambda_n)\}_{n\ge0}$ in $L^2(\Gamma)$ we have $\int\limits_\Gamma (\varphi \left( {t,{\lambda }_n }\right)-\tilde{\varphi} \left( {t,{\lambda }_n } \right)) f\left( t
\right)dt=0$.
By virtue of the arbitrariness of the function $f(x)$, we conclude that $\varphi \left({t,{\lambda }_n } \right)=\tilde{\varphi} \left( {t,{\lambda }_n }
\right)$ at all $n\geq 0$  and  $x\in\Gamma$. Consequently, $q(x)=\tilde{q}(x)$ almost everywhere on $\Gamma$ and $h=\tilde{h},\,H=\tilde{H}$. The theorem 6 is proven.


\so{Remark 2}.\  The results of the study will not change if the
boundary condi\-tions~(3) are replaced by the terms of Dirichlet
$y(0)_{\gamma_k} =0$ ($k=\overline{1,m-1}$), $y(\pi)_{\gamma_m}
=0$.


The totality of the results of the work [2] (inverse problem with distributed parameters on the simplest graph) and this work, indicates the path of analysis of the inverse spectral problem with distributed parameters on a arbitrary graph.
This path is based on the results of one of the authors of the article using the so-called ``gluing method'' of the linearly independent solutions of differential equations, defined on the subgraphs of this graph [11, 12].


\textbf{Example.}
In the capacity of  a illustration of the results we turn our attention to question  the restoration of elastic characteristics by a ``mast-stretching'' antenna design with one stretching strengthening  node to the body of the mast [10, 13].
In addition, the mast fragment, which is above the stretching strengthening  node, has a mass much smaller than the mass of the rest of the mast, that makes it possible to neglect it in a mathematical description of the process of vibrations of the mast system.
If this circumstance is taken into account, then mast fragment above the stretching strengthening  node is interpreted as a mass concentrated in the node equal to the mass of that fragment.
The mast fragment below the stretching strengthening  node experience longitudinal vibrations, and stretching  are transverse vibrations.

Let the variety $\Gamma$ is a graph-star with edges $\gamma_k \,\,(k=\overline{1,m})$ and a node $\xi$;  each edge $\gamma_k \,\,(k=\overline{1,m-1})$ is parameterized by a section $[0,\ell/2]$, a rib $\gamma_m$ is a section $[ \ell/2,\ell]$.
The mathematical model of the vibrating  process of the mast system is the initial-boundary value problem for the function $u(\zeta,t)$, $(\zeta,t)\in \Gamma \times[0,T]$  consisting of $m$ equations
\begin{equation}\label{eq14}
\frac{\partial ^2 u }{\partial t^2}(\zeta,t) =
\frac{\partial^2 u}{\partial\zeta^2}(\zeta,t) - q(\zeta) u (\zeta,t).
\end{equation}
On every edge $\gamma_k \,\,(k = \overline {1,m} )$, ratios
\begin{equation}\label{eq15}
u (\ell/2,t)_{\gamma_k }=u(\ell/2,t)_{\gamma_m },\quad \sum\limits_{k =
1}^{m - 1}  \frac{\partial u (\ell/2,t)_{\gamma_k }
}{\partial \zeta} = \frac{\partial u(\ell/2,t)_{\gamma_m }
}{\partial \zeta},
\end{equation}
in node $\xi$, initial and boundary conditions
\begin{equation}\label{eq2}
u (\zeta,0)=\tau(\zeta),\,\,\,\frac{\partial u }{\partial
t}(\zeta,0)=\hat{\tau}(\zeta),
\end{equation}
\begin{equation}\label{eq2}
\frac{\partial u }{\partial \zeta}(0,t)_{\gamma _k }
- h_k u (0,t)_{\Im_k }=0,\,\,\,
\frac{\partial u }{\partial\zeta}(\ell,t) + H u
(\ell,t)=0
\end{equation}
(function $q(x)$  describes the elastic characteristics of antenna fragments,  function $\tau(\zeta)$ and $\hat{\tau}(\zeta)$ determine the initial conditions of the vibrational process, constants $h$ and $H$ characterize the conditions of the stretching strengthening  node and body antenna, respectively).

The inverse problem for the system (14)--(17) is to determine the
parameters $q(x)$, $h$ и~$H$, that characterizes the elastic
properties of a ``mast-stretching'' antenna design. The
application of the Fourier method to the problem (14)--(17) and
consequent transformations analogous to the transformations of
Liouville  reduce to a inverse spectral problem (1)--(3) on the
graph-star $\Gamma$.



\textbf{Conclusion.} The paper considers the possibility of
restoring the  Sturm---Liouville operator with distributed
parameters on the graph-star on spectral characteristics, using
the classic results of the inverse spectral problems [8]. This
approach differs essentially from the approaches of the work [3,
4, 7], which use methods of the theory of boundary control of
differential systems. The conditions for the uniqueness solution
of the inverse spectral problem on the graph-star are obtained and
a example is given, illustrating the use of the obtained results
in solving inverse problems for evolutionary equations with
distributed parameters on the graph. It should be noted that the
obtained results supplement and develop the ideas and research
presented in the work [8] when analyzing the inverse problem for
the parabolic type equation with distributed parameters on the
simplest graph. These and the obtained results indicate the way in
which the inverse spectral problems  with distributed parameters
on arbitrary graph are analyzed. Recent results in the direction
of analysis evolutionary systems in network-like domains [14--20]
and analysis dynamic systems [21--26] allow the results of this
work to be used in the construction of
stabilizing\linebreak\newpage\noindent control effects in
complex-joint constructions. It should also be noted that the
approach to the analysis of relay control system presented in [27]
allow partial use of the results of this work.


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%\newpage
\input{05/ref-s-eng}% для английской статьи

%\newpage
\input{05/lit-ra-eng}% для английской статьи

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

{\footnotesize


%\thispagestyle{empty}

\vskip 3mm

\thispagestyle{empty} %очищаем стиль страницы
\thispagestyle{fancy} %включаем пользовательский стиль
\renewcommand{\headrulewidth}{0pt}%
\fancyhead[LO]{}%
\fancyhead[RE]{}%
\fancyfoot[LO]{\footnotesize{\rm{Вестник~СПбГУ.~Прикладная~математика.~Информатика...~\issueyear.~Т.~16.~Вып.~\issuenum}}
\hfill}%
\fancyfoot[RE]{\hfill\footnotesize{\rm{Вестник~СПбГУ.~Прикладная~математика.~Информатика...~\issueyear.~Т.~16.~Вып.~\issuenum}}}%
% для оформления нижнего колонтитула
\cfoot{} %

}
