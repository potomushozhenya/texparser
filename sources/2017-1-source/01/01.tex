%\begin{center}
%{\footnotesize
%\begin{tabular}{lcr}
%$\issueyear$   & ВЕСТНИК\,САНКТ-ПЕТЕРБУРГСКОГО\,УНИВЕРСИТЕТА  &  Том\,13.\,\,Вып.\,$\issuenum$ \\
%\multicolumn{3}{c}{ПРИКЛАДНАЯ МАТЕМАТИКА.
%ИНФОРМАТИКА. ПРОЦЕССЫ УПРАВЛЕНИЯ}\\
%\end{tabular}
%\end{center}
%}
{\footnotesize \noindent$\issueyear$\mbox{~~~~~~~~~~}
ВЕСТНИК\,САНКТ-ПЕТЕРБУРГСКОГО\,УНИВЕРСИТЕТА
Т.\,13.\,\,Вып.\,$\issuenum$\linebreak %
\mbox{~~~~~~~~~~}ПРИКЛАДНАЯ МАТЕМАТИКА. ИНФОРМАТИКА. ПРОЦЕССЫ
УПРАВЛЕНИЯ %
}

%\ \\ \vskip 0.8mm\hrule \\ \hrule \\ \ \\

\vskip 0.5mm

\hline\vskip .5mm

\hline

\vspace{1.2cm} \noindent {\large ПРИКЛАДНАЯ МАТЕМАТИКА}
\vspace{1.0cm}

\noindent{\footnotesize УДК 519.6}

\vskip2.0mm

\noindent{\it В.~И.~Ерохин$^1$, В.~В.~Волков$^2$}

\vskip3mm

\noindent{\bf О РЕГУЛЯРИЗОВАННОМ МЕТОДЕ НАИМЕНЬШИХ \\ КВАДРАТОВ
А.~Н.~ТИХОНОВА$^{*}$}

\efootnote{

\vspace{-3mm}\parindent=7mm


%{\copyright} Н. А. Валиотти, 2014

{\it Eрохин Владимир Иванович}~--- доктор физико-математических
наук, профессор; erohin\_v\_i@ mail.ru

{\it Волков Владимир Викторович}~--- кандидат
физико-математических наук, доцент; volkov@ bsk.vsu.ru



\vskip 2.0mm

\textit{Erokhin Vladimir Ivanovich}~--- doctor of physical and
mathematical sciences, professor; erohin\_v\_i@ mail.ru

\textit{Volkov Vladimir Viktorovich}~--- PhD of physical and
mathematical sciences, associate professor; volkov@bsk.vsu.ru

$^{*}$ Работа выполнена при финансовой поддержке Российского фонда
фундаментальных исследований (проект №~{16-31-50016} мол\_нр).

{\copyright} Санкт-Петербургский государственный университет,
\issueyear}

\vskip3mm

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\thispagestyle{empty} %очищаем стиль страницы
\thispagestyle{fancy} %включаем пользовательский стиль
\renewcommand{\headrulewidth}{0pt}%
\fancyhead[LO]{}%
\fancyhead[RE]{}%
%\fancyfoot[LO]{{\footnotesize\emph{\doivyp09 } }\hfill\thepage}%
\fancyfoot[LO]{{\footnotesize\rm{\doivyp/spbu10.\issueyear.\issuenum01 } }\hfill\thepage}%
%\fancyfoot[RE]{\thepage\hfill{\footnotesize\emph{\doivyp09 } } }%
\fancyfoot[RE]{\thepage\hfill{\footnotesize\rm{\doivyp/spbu10.\issueyear.\issuenum01}}}%
%\fancyfoot[LO]{\hfill{\fontsize{10.5}{10.5}\selectfont \thepage}}%
%\fancyfoot[RE]{{\fontsize{10.5}{10.5}\selectfont \thepage}\hfill}%
%\lhead{} %верхний колонтитул слева
%%\rhead{} % верхний колонтитул справа
% для оформления нижнего колонтитула
\cfoot{} %
%\lfoot{} %
%\rfoot{\thepage} %



%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

{\footnotesize


\noindent $^1$ Военно-космическая академия имени А.~Ф.~Можайского,
Российская Федерация,

\noindent\hskip2.45mm 197198, Санкт-Петербург, ул. Ждановская, 13

\noindent $^2$ Борисоглебский филиал ФГБОУ ВО <<Воронежский
государственный университет>>,

\noindent\hskip2.45mm Российская Федерация, 397160, Воронежская
обл., г. Борисоглебск, ул. Народная, 43


\vskip2.5mm

\begin{list}{}{\leftmargin=7mm \rightmargin=7mm \listparindent=5mm}

\item Рассматриваются регуляризованный метод наименьших квадратов А.~Н.~Тихонова
и его приложения к задачам обработки зашумленной информации.
Представлены априорные нижние оценки максимальной погрешности этого метода
и алгебраическое преобразование, понижающее его численную обусловленность.
Рассмотрен пример --- прикладная задача восстановления изображения,
приведены результаты вычислительных экспериментов.
Библиогр.~18~назв. Ил.~3.

{\it Ключевые слова}: приближенная система линейных алгебраических
уравнений, регуляризованный метод наименьших квадратов
А.~Н.~Тихонова.

\end{list}

}

%\newpage
\vskip3.0mm

\begin{list}{}{\leftmargin=7mm \rightmargin=7mm \listparindent=5mm}

\noindent{\it  V.~I.~Erokhin, V.~V.~Volkov}

\vskip2mm\noindent{\bf ABOUT A.~N.~TIKHONOV'S REGULARIZED \\ LEAST
SQUARES METHOD}

\vskip2mm



{\footnotesize

\noindent $^1$ Mozhaisky Military Space Academy, 13, Jdanovskaya
ul.,

\noindent\hskip2.45mm St.~Petersburg, 197198, Russian Federation


\noindent $^2$ Borisoglebsk branch of Voronezh State University,
43, Narondaya ul.,

\noindent\hskip2.45mm Borisoglebsk, 397160, Russian Federation


\vskip2mm


\item The Tikhonov's regularized least squares method (RLS) and its
applications to problems of noisy data processing are considered.
The starting point of this research is method for solving
approximate systems of linear algebraic equations (SLAE), proposed
by A. N. Tikhonov in 1980 that he later named 'RLS'. The
traditional method of finding sustainable solutions of the
problems with approximate data is Tikhonov's regularization:
unconditional minimization of the smoothing functional. RLS
differs from this approach. This method makes use of the solution
approach to the mathematical programming problem of a special
kind. Nowadays RLS has not become a common tool for solving
approximate linear systems, and the theory of RLS is not well
researched.  The purpose of the article is to remedy this
deficiency. A number of important theoretical and practical
aspects of RLS are considered. New results are presented. One of
the results is the method of constructing a model SLAE with the
exact right-hand side and approximate matrix for which a priori
lower bounds for the maximum relative error of RLS are achieved.
It is shown that, under certain conditions, RLS reduces to the
problem of minimizing the smoothing functional or to the least
squares method or~--- and this is an unexpected result~--- to the
problem of finding a stationary point of smoothing functional with
a negative regularization parameter. The algebraic transformation
which reduces the conditionality of the RLS problem to a numerical
solution is constructed and substantiated.  We propose
illustrative example of RLS through an applied image restoration
problem for an image, registered by a device with an inexact
point-spread function. The results of computational experiments
are given. Refs~18. Figs~3.

\textit{Keywords}: the approximate system of linear algebraic
equations, regularized least squares method.


}

\end{list}

\vskip 4mm



%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


\textbf{Введение.} В настоящей работе рассматривается метод
решения приближенных систем линейных алгебраических уравнений
(СЛАУ), предложенный А.~Н.~Тихоновым в работах~[1,~2]
%\cite{Tihonov_GVM_1980, Tihonov_AN_1980},
и позже названный им регуляризованным методом наименьших квадратов
(РМНК)~[3].
%\cite{Tihonov_VAN_1983}:

\textbf{Задача~$P_0(\mu, \delta)$}~(РМНК). Пусть существует
гипотетическая точная совместная СЛАУ
\begin{equation}
\label{eq_sistem} A_0 x = b_0,
\end{equation}
где $A_0 \in \mathbb{R}^{m\times n}$; $b_0 \in \mathbb{R}^m$; $b_0
\ne 0$; соотношения между размерами $A_0$ и ее рангом не
оговариваются; $x_0 \in \mathbb{R}^n$~--- решение системы
\eqref{eq_sistem} с минимальной евклидовой нормой (нормальное
решение). Численные значения $A_0$ и $b_0$ неизвестны, а вместо
них заданы приближенные матрица $A \in \mathbb{R}^{m\times n}$ и
вектор $b \in \mathbb{R}^m$, $b \ne 0$, такие, что выполняются
условия
\begin{equation} \label{A_cond}
\|A_0 - A\| \leqslant \mu,
\end{equation}
\begin{equation} \label{b_cond}
\|b_0 - b\| \leqslant \delta < \|b\|.
\end{equation}

В неравенствах \eqref{A_cond} и \eqref{b_cond} $\mu \geqslant 0$ и
$\delta \geqslant 0$ "--- известные параметры, одновременно не
равные нулю, символом $\|\cdot \|$ обозначена, в зависимости от
контекста, матричная или векторная евклидова норма. Полнота ранга
матрицы $A$ и совместность системы
\begin{equation}
\label{Axb} Ax = b
\end{equation}
в общем случае не предполагаются.

{Требуется найти $A_1 \in \mathbb{R}^{m \times n}$, $b_1 \in
\mathbb{R}^m$, $x_1 \in \mathbb{R}^n$ такие, что $\|A - A_1\|
\leqslant \mu$, $\|b - b_1\| \leqslant \delta$, $A_1 x_1 = b_1$,
$\|x_1\| \to \min$.}

Как было показано в работах~[1,~2],
%\cite{Tihonov_GVM_1980, Tihonov_AN_1980}
вектор $x_1$ является устойчивым приближением к вектору $x_0$,
т.~е. $\mathop{\lim}\limits_{\mu, \delta \to 0} x_1 = x_0$.

Кроме того, в~[1,~2]
%\cite{Tihonov_AN_1980, Tihonov_GVM_1980}
был предложен способ решения задачи $P_0 (\mu, \delta)$, не
ставший стандартным для сложившейся в последнее время теории и
практики решения приближенных СЛАУ. Указанный способ заключается в
переходе от задачи $P_0(\mu, \delta)$ к~задаче математического
программирования: %\textbf{
\begin{equation}
\label{ZP1} P_1(\mu, \delta): \|x\| \to \mathop{\min}\limits_{\|b
- Ax\| = \mu \|x\| + \delta}.
\end{equation}
%}

%\begin{statement}
\textbf{Утверждения}~[1,~2].
%\cite{Tihonov_AN_1980, Tihonov_GVM_1980}
%\label{st1}

\textit{1. Задача $P_1(\mu, \delta)$ имеет решение тогда и только
тогда, когда существует хотя~бы одна совместная СЛАУ $\breve{A} x
= \breve{b}$, для которой $\|\breve{A} - A\| \leqslant \mu$,
$\|\breve{b} - b\| \leqslant \delta$.}\newpage

\textit{2. Если решение задачи $P_1(\mu, \delta)$ существует, то
оно единственное при $\mu, \delta \to 0$.}

\textit{3. Если $x_{\mu \delta}$ "--- решение задачи $P_1(\mu,
\delta)$, то решение задачи $P_0(\mu, \delta)$ имеет вид
\[
x_1 = x_{\mu \delta}, \quad b_1 = b - \frac{\delta}{\|b - Ax_{\mu
\delta}\|}( b - Ax_{\mu \delta}), \quad A_1 = A + (b_1 - Ax_{\mu
\delta}) \frac{x_{\mu \delta}^T}{x_{\mu \delta}^T x_{\mu \delta}}.
\]
При этом $x_{\mu \delta}$ есть решение системы $A_1 x = b_1$ с
минимальной евклидовой нормой и выполняются условия $\|A_1 - A\| =
\mu$, $\|b_1 - b\| = \delta$. }

%\end{statement}

%\begin{statement}
%\textbf{Утверждение 2}~[1,~2].
%\cite{Tihonov_AN_1980, Tihonov_GVM_1980}]
%\label{st2}
%\textit{Справедливо равенство
%\[
%x_1 = x_{\mu \delta}.
%\]
%}
%\end{statement}


Более традиционным~[4--6]
%\cite{Tihonov_Arsenin, Ivanov_Vasin_Tanana, Tihonov_Goncharsky}
способом решения приближенных СЛАУ является безусловная
минимизация сглаживающего функционала А.~Н.~Тихонова при некотором
заданном положительном значении скалярного параметра $\alpha$:

%\textbf{
\begin{equation}
\label{functional} P_2(\alpha): \Phi^\alpha(x, A, b) = \|b -
Ax\|^2 + \alpha \|x\|^2 \to \mathop{\min}\limits_x.
\end{equation}
%}

%\textbf{Задача~$P_2$}: $\Phi^\alpha(x, A, b) \to \mathop{\min}\limits_x$.


Будем обозначать через $z_\alpha$ решение задачи $P_2(\alpha)$.

Известно (см., например,~[7]),
%\cite{Voevodin_Kuznecov}),
что при любом $\alpha > 0$ существует единственный вектор
$z_\alpha$. Кроме того, при соответствующем $\alpha > 0$ вектор
$z_\alpha$~--- устойчивое приближение к вектору $x_0$, т.~е. он
представляет собой решение задачи $P_0$ при $\mu,\delta \to 0$
[4--6].
%\cite{Tihonov_Arsenin, Ivanov_Vasin_Tanana, Tihonov_Goncharsky}.

%
Рассмотрим систему
\begin{equation} \label{sist_alpha}
(A^T A + \alpha I_n)x = A^T b,
\end{equation}
в которой $I_n$~--- единичная матрица порядка $n$.
%.
Введем обозначения: $x_\alpha$ "--- решение системы
\eqref{sist_alpha} при любом заданном $\alpha \in \mathbb{R}$,
$x_{\alpha+}$~--- решение системы \eqref{sist_alpha} при $\alpha
\geqslant 0$; $x_{\alpha-}$~--- решение системы \eqref{sist_alpha}
при $\alpha < 0$, $\hat{x}$~--- нормальное псевдорешение системы
\eqref{Axb} и~в~то же время нормальное решение системы
\eqref{sist_alpha} при $\alpha = 0$, т.~е. $\hat{x}=A^{+}b$, где
$A^{+}$~--- псевдообратная матрица [7].

%Как удалось показать~[8],
%\cite{Volkov_Erohin_GVM_2010},
Справедливы следующие утверждения.

\textbf{Лемма~1}. \textit{Стационарные точки функционала
\eqref{functional} являются решениями СЛАУ~\eqref{sist_alpha}.}

Справедливость леммы непосредственно следует из условия равенства
нулю гра\-диен\-та $\Phi^{\alpha}(x,A,b)$.

\textbf{Лемма~2} [8]. \textit{Если решение задачи
$P_1(\mu,\delta)$ существует, то найдется значение $\alpha$ такое,
что $x_{\mu,\delta} = x_{\alpha}$.}


%\begin{theorem}
\textbf{Теорема~1} [8]. \textit{Если задача $P_1(\mu, \delta)$
имеет решение $x_{\mu \delta}$, то возможны такие случаи:}

\textit{1) eсли выполняется условие $\|b - A\hat{x}\| < \mu
\|\hat{x}\| + \delta$, то вектор $x_{\mu \delta}$ есть
единственное решение задач $P_0(\mu, \delta)$, $P_1(\mu, \delta)$
и $P_2(\alpha)$ при некотором $\alpha > 0$};
%(т.е. решение задачи $P_1(\mu, \delta)$ совпадает с решением задачи $P_2$).}

\textit{2) eсли выполняется условие $\|b - A\hat{x}\| = \mu
\|\hat{x}\| + \delta$, то вектор $\hat{x} = x_{\mu \delta}$~---
единственное решение задач $P_0(\mu, \delta)$, $P_1(\mu, \delta)$
и $P_2(\alpha)$ при $\alpha = 0$};

\textit{3) eсли выполняется условие $\|b - A\hat{x}\| > \mu
\|\hat{x}\| + \delta$, то вектор $x_{\mu \delta}$ является
%решением
%задачи $P_1(\mu, \delta)$, а также
ста\-цио\-нар\-ной точкой функционала \eqref{functional} при
$\alpha \leqslant 0$. Если матрица $A$ имеет полный столбцевой
ранг, то $\alpha < 0$
%(т.е. решение задачи $P_1(\mu, \delta)$ является решением
%некоторой задачи минимальной матричной коррекции) и
и вектор $x_{\mu \delta}$~--- единственное решение задач $P_0(\mu,
\delta)$ и~$P_1(\mu, \delta)$. В противном случае $\alpha = 0$,
решение задач $P_0(\mu, \delta)$ и $P_1(\mu, \delta)$ не
единственно и имеет вид $x_{\mu \delta} = \hat{x} + \Delta x$, где
$\Delta x$~--- произвольный вектор, такой, что $A \Delta x = 0$,
$\|b - A\hat{x}\| = \mu \|\hat{x} + \Delta x\| + \delta$. }
%\end{theorem}


В данной работе рассмотрен ряд важных теоретических и практических
аспектов РМНК. В п.~1 исследованы модельные СЛАУ, для которых
решения задачи $P_1(\mu, \delta)$ могут оказаться достаточно
далеки от нормального решения $x_0$ гипотетической точной системы
\eqref{eq_sistem}.
%максимальные относительные погрешности решения задачи  и получены их априорные нижние оценки.
В п.~2 строится и обосновывается алгебраическое преобразование,
снижающее обусловленность решения задачи РМНК. В п.~3,~4
рассматривается пример РМНК~--- прикладная задача восстановления
изображения, зарегистрированного прибором с неточно заданной
аппаратной функцией, представлены результаты соответствующих
вычислительных экспериментов.


\textbf{1. Априорные нижние оценки максимальной относительной
погрешности решения задачи РМНК.} Оценивание погрешности решения
приближенной СЛАУ относится к важным теоретическим и прикладным
проблемам. Априорные оценки погрешности классического метода
наименьших квадратов хорошо известны~[7,~9],
%\cite{Lowson_Henson, Voevodin_Kuznecov}
чего нельзя утверждать о РМНК.

Уточним постановку проблемы. Рассмотрим вектор $\Delta x = x_0 -
x_1$ отклонений решения $x_0$ гипотетической точной СЛАУ
\eqref{eq_sistem} от решения $x_1$ соответствующей задачи РМНК.
Величину $\|\Delta x\|$ будем называть \textit{абсолютной
погрешностью} решения задачи РМНК, а $\varepsilon = \frac{\|\Delta
x\|}{\|x_0\|}$~--- \textit{относительной погрешностью}. С учетом
вышесказанного рассмотрим проблему построения наихудшей нижней
оценки относительной погрешности решения задачи РМНК, которая
может быть формализована как проблема построения двух модельных
СЛАУ~--- точной и приближенной~--- таких, что значение
$\varepsilon$ оказывается максимальным.


%Попробуем определить условия,
%при которых погрешность решения задачи РМНК максимальна. Определимся с необходимыми %терминами. Вектор $укуку$

%\[
%\varepsilon = \|x_0 - x_1\| = \|x_0 - x_{\mu \delta}\| \to \max.
%\]

Ввиду сложности указанной проблемы ограничимся случаем, когда
правая часть исследуемой приближенной СЛАУ свободна от
погрешности, т.~е. $\delta = 0$, а термин РМНК применяется к
задачам $P_0(\mu, 0)$ и $P_1(\mu, 0)$.

Для практики наибольший интерес представляет случай, когда матрица
$A$ приближенной СЛАУ имеет полный столбцевой ранг, а ее
погрешность $\mu$ меньше ее минимального сингулярного числа
$\sigma_{\min}$. Поэтому изучим именно этот случай.

Следующие теоремы дают априорные нижние оценки максимальной
относительной погрешности решения задачи РМНК для различных
случаев <<поведения>>  приближенной СЛАУ, рассмотренных в
теореме~1.

%\begin{theorem}
\textbf{Теорема~2.} \textit{ Для случая $\|b_0 - A\hat{x}\| <
\mu\|\hat{x}\|$, $\rank A = n$, $\mu < \sigma_{\min}$ максимальное
значение относительной погрешности решения задачи РМНК
%(где $\Delta x = x_0 - x_\mu$)
будет не меньше, чем величина}
\begin{equation*} %\label{pogr_1}
\varepsilon _1 = \frac{2\mu}{\sigma_{\min} + \mu}.
\end{equation*}
%\end{theorem}


%\begin{theorem}
\textbf{Теорема~3.} \textit{ Для случая $\|b_0 - A\hat{x}\| >
\mu\|\hat{x}\|$, $\rank A = n$,  $\mu < \sigma_{\min}$
максимальное значение относительной погрешности решения задачи
РМНК
%(где $\Delta x = x_0 - x_\mu$)
будет не меньше, чем величина}
\begin{equation*} %\label{pogr_2}
\varepsilon_2 = \frac{2\mu^4}{\sigma_{\min}^4 + \mu^4}.
\end{equation*}
%\end{theorem}

%\begin{theorem}
\textbf{Теорема~4.} \textit{ Для случая $\|b_0 - A\hat{x}\| = \mu
\|\hat{x}\|$, $\rank A = n$, $\mu < \sigma_{\min}$ максимальное
значение относительной погрешности решения задачи РМНК
%(где $\Delta x = x_0 - x_\mu$)
будет не меньше, чем величина}
\begin{equation} \label{pogr_3}
\varepsilon_3 = \frac{2\mu^2}{\sigma_{\min}^2 + \mu^2}.
\end{equation}
%\end{theorem}


Доказательство теорем~2 и 3 приведено в работе~[8]. Доказательство
теоремы~4 вошло в диссертационное исследование~[10], но ранее не
публиковалось, поэтому ниже оно приведено полностью.


%\begin{proof}
%Перейдем теперь к доказательству  теоремы~4.

Д\,о\,к\,а\,з\,а\,т\,е\,л\,ь\,с\,т\,в\,о. Покажем, что существуют
удовлетворяющие условиям теоремы модельная точная и
соответствующая ей оптимальная (в смысле РМНК) приближенная СЛАУ,
для которых оценка \eqref{pogr_3} достигается. Для модельной
точной СЛАУ укажем способ построения матрицы $A_0$, векторов $b_0$
и $x_0$, обоснуем существование указанных объектов, а также то,
что вектор $x_0$ есть единственное решение системы
\eqref{eq_sistem}. В силу утверждения~1, содержащего методику
построения оптимальной приближенной СЛАУ по решению задачи РМНК,
покажем (и этого достаточно), что модельный вектор $x_{\mu}$
действительно является решением соответствующей задачи
$P_1(\mu,0)$, а матрицы $A_0$ и $A$ удовлетворяют условию
\eqref{A_cond}.

1. Приведем расчетные формулы для построения объектов $A$, $A_0$,
$b_0$, $x_0$, $x_{\mu}$ и~убедимся, что эти объекты действительно
могут быть построены.

Пусть $A \in \mathbb{R}^{m\times n}$~--- произвольная матрица,
такая, что  $n < m$, $\rank A = n$ (следовательно, $\sigma_{\min}
> 0$), $x_\mu \ne 0$~--- собственный вектор матрицы $A^TA$,
соответствующий ее минимальному собственному значению
$\lambda_{\min}(A^TA) = \sigma_{\min}^2$; $0 < \mu <
\sigma_{\min}$; параметры точной системы рассчитываются следующим
образом:
\begin{equation}
\label{E_Sys} x_0 = \frac{\sigma_{\min}^2 +\mu^2}{\sigma_{\min}^2
- \mu^2} \cdot x_\mu, \quad b_0 = Ax_\mu + \Delta b, \quad A_0 = A
+ (b_0 - Ax_0)\frac{x_0^T}{x_0^T x_0},
\end{equation}
причем выполнены условия
\begin{equation}
\label{CND} \|\Delta b\| = \mu \|x_\mu\|, \quad A^T\Delta b = 0.
\end{equation}

Убедимся в существовании объектов, задаваемых формулами
\eqref{E_Sys},~\eqref{CND}. Матрица $A$ и векторы $x_0$, $x_\mu$,
очевидно, существуют, причем из условия $x_\mu \ne 0$ следует
условие $x_0 \ne 0$, достаточное для существования матрицы $A_0$.
Поскольку $n < m$, возможно существование ненулевого вектора
$\Delta b$, удовлетворяющего условию $A^T \Delta b = 0$. Таким
образом, соответствующим выбором величины $\|\Delta b\|$ можно
обеспечить выполнение условия $\|\Delta b\| = \mu \|x_\mu\|$.

Следовательно, все рассматриваемые объекты $A$, $A_0$, $b_0$,
$x_0$, $x_{\mu}$ на самом деле существуют.


%Покажем, что $A_0$ отвечает условиям теоремы 4 при $Z = 0$. Очевидно, что $Zx_0 = 0$.
2. Заметим, что из \eqref{E_Sys} непосредственно вытекает, что
вектор $x_0$ действительно есть решение СЛАУ \eqref{eq_sistem}.

3. Покажем, что
\begin{equation}
\label{A_cond=} \|A_0-A\| = \frac{\|b_0 - Ax_0\|}{\|x_0\|} = \mu.
\end{equation}

В силу \eqref{E_Sys},~\eqref{CND} и того факта, что вектор
$x_{\mu}$ является собственным вектором матрицы $A^T A$,
отвечающим ее минимальному собственному значению
$\sigma_{\min}^2$, имеем
%Для этого рассмотрим норму разности $\|b_0 - Ax_0\|^2$, подставив выражения для $b_0$ и $x_0$ (в выкладках %учтем, что
%$A^T \Delta b = 0$, а $x_\mu$ "--- собственный вектор матрицы $A^TA$, соответствующий ее минимальному %собственному значению):
\[
\|b_0 - Ax_0\|^2 = \|Ax_\mu + \Delta b - Ax_0\|^2 = \|A(x_\mu -
x_0) + \Delta b\|^2 =
\]
\[
= \left\|\left(1 - \frac{\sigma_{\min}^2 + \mu^2}{\sigma_{\min}^2
- \mu^2}\right) Ax_\mu + \Delta b \right\|^2 =
\left\|\frac{-2\mu^2}{\sigma_{\min}^2 - \mu^2}Ax_\mu + \Delta
b\right\|^2 =
\]
\[
= \frac{4\mu^4}{(\sigma_{\min}^2 - \mu^2)^2}\|Ax_\mu\|^2 +
\|\Delta b\|^2 = \mu^2\left(\frac{4\mu^2}{(\sigma_{\min}^2 -
\mu^2)^2} \|Ax_\mu\|^2 + \|x_\mu\|^2\right) =
\]
\[
= \mu^2\|x_\mu\|^2
\left(\frac{4\mu^2\sigma_{\min}^2}{(\sigma_{\min}^2-\mu^2)^2} +
1\right) = \mu^2\|x_\mu\|^2 \left(\frac{\sigma_{\min}^2 +
\mu^2}{\sigma_{\min}^2 - \mu^2}\right)^2 = \mu^2\|x_0\|^2,
\]
откуда и получаем равенство \eqref{A_cond=}. Сопоставив
\eqref{A_cond} и \eqref{A_cond=}, убеждаемся, что матрицы $A_0$ и
$A$ отвечают условию \eqref{A_cond} задачи РМНК.

%То есть $\frac{\|b_0 - Ax_0\|^2}{\|x_0\|^2} = \mu^2$, а, следовательно, $\|A_0 - A\| = \mu$.

4. Заметим, что совокупность условий $\rank A = n$,
$\mu<\sigma_{\min}$, $\|A_0 - A\|<\mu$ гарантирует выполнение
условия $\rank A_0 = n$~[11].
%[Хорн-Джонсон].

Это, в свою очередь, означает, что вектор $x_0$ "--- единственное
решение СЛАУ \eqref{eq_sistem}, которое, в силу единственности,
является нормальным.

5. Из совокупности условий $b_0 = Ax_{\mu} + \Delta b$ и $\|\Delta
b\|=\mu\|x_{\mu}\|$ напрямую следует равенство
\begin{equation}
\label{ZP1=} \|b_0 - Ax_\mu\| = \mu \|x_\mu\|.
\end{equation}
Сопоставив \eqref{ZP1} и \eqref{ZP1=}, убеждаемся, что вектор
$x_{\mu}$ принадлежит допустимому множеству задачи $P_1(\mu,0)$.


6. Рассмотрим СЛАУ $A^Tb_0 = (A^TA + \alpha I)x$. Учтя равенство
$b_0 = Ax_\mu + \Delta b$ и~условие $A^T \Delta b = 0$, получаем,
что вектор $x_\mu$ есть решение указанной СЛАУ при $\alpha = 0$.
Следовательно, в силу \eqref{ZP1=}, лемм~1, 2 и теоремы~1, вектор
$x_{\mu}$ является решением задачи $P_1(\mu,0)$, соответствующим
случаю 2 теоремы~1.

Совокупность утверждений, обоснованных в п. 1--6 позволяет сделать
вывод, что векторы $x_0$, $x_\mu$, $b_0$, $\Delta b$ и матрицы
$A$, $A_0$ действительно существуют и отвечают условиям задачи
$P_0(\mu,0)$.
%Следовательно,
%СЛАУ $A_0 x = b_0$ и $Ax = b_0$ действительно являются
%соответственно точной и приближенной системами, отвечающими условиям задачи $P_0(\mu, 0)$.

7. Покажем, что оценка \eqref{pogr_3} достигается как равенство на
векторах $x_0$ и $x_{\mu}$.
%$\frac{\|x_0 - x_\mu\|}{\|x_0\|} = \frac{2\mu^2}{\sigma_{\min}^2 + \mu^2}$.
Действительно,
\[
\frac{\|x_0 - x_\mu\|}{\|x_0\|} = \frac{\left\|x_0 -
\frac{\sigma_{\min}^2 - \mu^2}{\sigma_{\min}^2 +
\mu^2}x_0\right\|}{\|x_0\|} = 1 - \frac{\sigma_{\min}^2 -
\mu^2}{\sigma_{\min}^2 + \mu^2} = \frac{2\mu^2}{\sigma_{\min}^2 +
\mu^2}.
\]
Теорема доказана. \hfill $\square$
%\end{proof}


%\begin{remark}
\textbf{Замечание}.\label{rem_pogr} \textit{ Из теорем 2--4
вытекает, что существуют такие задачи РМНК, для
которых %во всех трех случаях, охватываемых теоремой~1 относительная погрешность решения задачи РМНК стремится к %единице при стремлении  $\mu$ снизу к $\sigma_{\min}$.
\[\mathop {\lim }\limits_{\mu  \to {\sigma _{\min }}^- } {\varepsilon _k} = 1,\quad k = 1,2,3.\]
}
%\end{remark}

\textbf{2. Обусловленность задачи РМНК.}
Из теоремы 1 следует, что  РМНК %должен давать устойчивое решения системы \eqref{sist_alpha}
может использоваться
при %всех возможных
различных значениях параметра $\alpha$: при $\alpha>0$
(классическая регуляризация); при $\alpha \rightarrow 0_+$
(классический метод наименьших квадратов); при $\alpha<0$
(обобщенный метод наименьших квадратов)~[8,~12].
%\cite{Golub_Hansen_OLeary}.

Для
%первого и второго случаев, рассмотренных в теореме 1, ($\alpha>0$ и $\alpha \rightarrow 0_+$) А.~И.~Ждановым в %работе~[13]
%\cite{ZHdanov_2012}
%с целью
уменьшения вычислительных погрешностей решения СЛАУ
\eqref{sist_alpha} при $\alpha > 0$
 %при построении псевдорешений переопределенных СЛАУ
в~работе~[13] был предложен прием ее сведения к следующей
эквивалентной системе:
\begin{equation} \label{sZH}
\left[
\begin{array}{cc}
  \sqrt{\alpha}\cdot I_m & A                       \\
  A^T                    & -\sqrt{\alpha}\cdot I_n \\
\end{array}
\right] \cdot \left[
\begin{array}{c}
  z          \\
  x_{\alpha} \\
\end{array}
\right] = \left[
\begin{array}{c}
  b   \\
  0_n \\
\end{array}
\right],
\end{equation}
в которой $I_m$ "--- единичная матрица порядка $m$, $I_n$ "---
единичная матрица порядка $n$, $0_n$ "--- нулевой вектор порядка
$n$, $z=\frac{1}{\sqrt{\alpha}}(b-A x_{\alpha})$. Матрицу системы
\eqref{sZH} обозначим $A_{Zh}$.


Как было показано в~[13], число обусловленности матрицы $A_{Zh}$
по спектральной норме

\[\operatorname{cond} A_{Zh} = \frac{{{{\left\| A_{Zh} \right\|}_2}}}{{{{\left\| {{A_{Zh}^{ - 1}}} \right\|}_2}}} = \frac{{\left| {{\lambda _{\max }}} \right|}}{{\left| {{\lambda _{\min }}} \right|}},\]% MathType!End!2!1!
%\cite{ZHdanov_2012},
где $\|\cdot\|_2$ "--- спектральная норма; $\lambda_{\min}$,
$\lambda_{\max}$ "--- минимальное и максимальное собственное
значение указанной матрицы, оказывается меньше числа
обусловленности матрицы системы \eqref{sist_alpha} для любого
$\alpha > 0$, а для вычисления собственных значений матрицы
$A_{Zh}$ при $\alpha > 0$ может быть использована следующая
теорема.

%\begin{theorem}
\textbf{Теорема~5.}

\textit{ 1. Если $A = O_{m \times n}$ "--- нулевая $m \times
n$-матрица, то собственные значения расширенной матрицы $A_{Zh}$
есть числа $\sqrt{\alpha}$ и $-\sqrt{\alpha}$ кратностей $m$ и $n$
соответственно.}

\textit{ 2. Если $A \neq O_{m \times n}$, тогда собственными
значениями матрицы $A_{Zh}$ являются числа $\pm\sqrt{\sigma^2_i +
\alpha}$, $i=1,2,\ldots,r$, а также   $\sqrt{\alpha}$ и
$-\sqrt{\alpha}$ кратностей $m-r$ и $n-r$ соответственно, где
$r=\rank A \geqslant 1$, $\sigma_i$ "--- сингулярные числа матрицы
$A$, причем $\sigma_1 = \sigma_{\rm max} \geqslant \sigma_2
\geqslant \ldots \geqslant \sigma_r > 0$. }
%\end{theorem}

Случай $\alpha < 0$ в работе~[13]
% \cite{ZHdanov_2012}
не рассматривался. Однако существуют приближенные СЛАУ, в которых
именно при отрицательных значениях параметра регуляризации
полученное решение оказывается наиболее близким к решению точной
системы~[12,~14].
%\cite{Golub_Hansen_OLeary, GE2004}.

%Рассмотрим случай $\alpha < 0$.
Отметим, что, в отличие от случая $\alpha > 0$, матрица $A_{Zh}$
при $\alpha <0$ неэрмитова, что затрудняет поиск ее сингулярных
чисел и оценивание числа обусловленности.

Введем в рассмотрение еще одну систему линейных алгебраических
уравнений:
\begin{equation} \label{sZHm}
\left[
\begin{array}{cc}
  \sqrt{-\alpha}\cdot I_m & A                       \\
  A^T                      & \sqrt{-\alpha}\cdot I_n \\
\end{array}
\right] \cdot \left[
\begin{array}{c}
  z          \\
  x_{\alpha} \\
\end{array}
\right] = \left[
\begin{array}{c}
  b   \\
  0_n \\
\end{array}
\right],
\end{equation}
где $z=\frac{1}{\sqrt{-\alpha}}(b-A x_{\alpha})$.

Матрицу системы \eqref{sZHm} обозначим $\tilde{A}_{Zh}$. Несложно
показать, что при $\alpha \neq 0$ система \eqref{sZHm} также
эквивалентна системе \eqref{sist_alpha}, причем при $\alpha < 0$
матрица $\tilde{A}_{Zh}$ и вектор решения системы \eqref{sZHm}
являются вещественными.

Как было показано в работе~[15],
%\cite{EVB_ISTI_2014},
для матрицы $\tilde{A}_{Zh}$ при $\alpha < 0$ справедлива

%\begin{theorem}
\textbf{Теорема~6.} \textit{ Пусть $A \in \mathbb{R}^{m\times n}$,
$\rank A = r \geqslant 1$, $\alpha < 0$. Тогда при выполнении
условия $m = n = r$ собственные значения матрицы $\tilde{A}_{Zh}$
образуют множество
\[
\left\{ \sqrt{|\alpha|} + \sigma_1, \ldots, \sqrt{|\alpha|} +
\sigma_r, \sqrt{|\alpha|} - \sigma_1, \ldots , \sqrt{|\alpha|} -
\sigma_r \right\},
\]
а если условие $m = n = r$ не выполняется "--- то множество
\[
\left\{ \sqrt{|\alpha|} + \sigma_1, \ldots, \sqrt{|\alpha|} +
\sigma_r, |\alpha|, \ldots, |\alpha|, \sqrt{|\alpha|} - \sigma_1,
\ldots , \sqrt{|\alpha|} - \sigma_r \right\},
\]
причем число $|\alpha|$ есть собственное значение матрицы
$\tilde{A}_{Zh}$ кратности $m + n - 2r$. }
%\end{theorem}

%Можно показать, что при определенных условиях числа обусловленности систем \eqref{sZH} и \eqref{sZHm} меньше числа %обусловленности системы \eqref{sist_alpha}.

Как показывают вычисления (доказательство разрабатывается), при
${-\sigma_{\min}^2<$ $\alpha<0}$ выполняется условие $
\operatorname{cond} \tilde{A}_{Zh} $ $ {< \operatorname{cond}(A^T
A +\alpha I)} $, если матрица $A$ имеет полный столбцевой ранг и
{$\operatorname{cond} A>2$}.


Таким образом, для вычислительной реализации РМНК, обладающей
минимальной обусловленностью, можно предложить использовать
решение системы \eqref{sZH} при $\alpha > 0$ и решение системы
\eqref{sZHm} при $\alpha < 0$.
%Для краткости введем обозначение:
%\[
%A_{\omega} =
%\left\{
%  \begin{array}{c}
%    A_{Zh} \text{ при } \alpha > 0; \\
%    \tilde{A_{Zh}}  \text{ при } \alpha < 0.
%  \end{array}
%\right..
%\]

\textbf{3. Задача восстановления исходного изображения по
изображению, зарегистрированному прибором с неточной аппаратной
функцией.} Пусть имеется некоторая техническая система,
регистрирующая изображения. Как правило~[16,~17],
%\cite{Sizikov, Voskoboinikov_Litasov},
связь между исходным изображением $x(\tau_1, \tau_2)$  и
зарегистрированным изображением $b(t_1, t_2)$ может быть описана
интегральным уравнением Фредгольма I рода типа свертки (с
разностным ядром) следующего вида (см., например,~[16]):
%\cite{Sizikov}):
\begin{equation} \label{fredgolm2d}
\int\limits_{c_1}^{d_1} \int\limits_{c_2}^{d_2} h(t_1 - \tau_1,
t_2 - \tau_2) x(\tau_1, \tau_2)\,d\tau_1 d\tau_2 = b(t_1, t_2),
\end{equation}
где $h(\tau_1, \tau_2)$~--- аппаратная функция регистрирующей
системы, представляющая\linebreak\newpage\noindent собой ядро
интегрального уравнения (в идеальном случае~--- $\delta$-функция);
$t_1$, $t_2$ и $\tau_1$, $\tau_2$~--- соответственно
горизонтальные и вертикальные координаты точек изображения; $x$  и
$b$~--- матрицы интенсивности цвета в соответствующих точках
изображения; при исследовании монохромного (в оттенках серого)
изображения и его хранении в однобайтовом беззнаковом формате
элементы этих матриц принимают целочисленные значения в диапазоне
от 0 до 255.

На практике нередко регистрацию изображения затрудняют внешние
факторы:

--- аппаратная функция $h(\tau_1, \tau_2)$ может отличаться от
$\delta$-функции в силу конечной полосы пропускания регистрирующей
системы;

--- вместо $b(t_1, t_2)$ регистрируется зашумленная правая часть
$\tilde{b}(t_1, t_2) = b(t_1, t_2) + \eta(t_1, t_2)$;

--- вместо $h(\tau_1, \tau_2)$ может быть известно только ее
приближенное значение $\tilde{h}(\tau_1, \tau_2) = h(\tau_1,
\tau_2) + \xi(\tau_1, \tau_2)$  .


Третий фактор может наблюдаться, например, если функция $h(\tau_1,
\tau_2)$  оцени\-вается при помощи процедуры непараметрической
идентификации или она изме\-няет\-ся под воздействием случайных
неконтролируемых факторов или воздействий.

В общем виде задача восстановления изображения,
зарегистрированного прибором с неточно заданной аппаратной
функцией, может быть сформулирована сле\-дую\-щим образом: по
заданным приближенным значениям $\tilde{h}(\tau_1, \tau_2)$ и
$\tilde{b}(t_1, t_2)$ оценить значение исходного изображения
$x(\tau_1, \tau_2)$.

Некоторые подходы к решению задачи в таком виде рассмотрены, к
примеру, в работе~[17],
%\cite{Voskoboinikov_Litasov},
а более общие постановки задач "--- в монографии~[16].
%\cite{Sizikov}.

Конкретизируем постановку задачи, приняв ряд дополнительных
предположений (исследованию задачи в такой постановке посвящена
работа~[18]):

1) регистрация изображения происходит построчно (аппаратная
функция одномерна);

2) аппаратная функция представляет собой $\delta$-функцию
(зарегистрированное изображение не подвергается дефокусировке,
<<размытию>>), но вместо ее точного значения известно только
приближенное (на аппаратную функцию наложен шум, что приводит
к~построчному <<смазыванию>> изображения);

3) правая часть $b(t_1, t_2)$ регистрируется точно (без шума).

С учетом первого предположения вместо решения двумерного
интегрального уравнения \eqref{fredgolm2d} задача сводится к
решению набора одномерных интегральных уравнений (для каждой
строки изображения). В этом случае связь между строкой исходного
изображения $x_{t_i}$ и строкой зарегистрированного изображения
$b_{t_i}$ может быть описана одномерным интегральным уравнением
Фредгольма I рода типа свертки
\begin{equation} \label{fredgolm}
\int\limits_{c}^{d} h(t_2 - \tau) x_{t_1}(\tau)\,d\tau =
b_{t_1}(t_2),
\end{equation}
где $h(t_2 - \tau)$~--- одномерная аппаратная функция
регистрирующей системы, известная с некоторой ошибкой. Решить
уравнение \eqref{fredgolm} относительно $x_{t_1}(\tau)$ означает
по значениям $b_{t_1}(t_2)$ оценить значения входного изображения
$x_{t_1}(\tau)$ при неточно заданном ядре $h(t_2 - \tau)$.

Уравнение \eqref{fredgolm} может быть сведено к системе линейных
алгебраических уравнений путем дискретизации (как правило,
используется попиксельная дискретизация):

\begin{equation*} %\label{imgSum}
\sum_{k=1}^{n+l-1} h_k x_{i, k} = b_{i, j}, \quad i=1,\ldots,m,
\quad j=1,\ldots,n+l-1,
\end{equation*}
или в матричной форме
\begin{equation} \label{imgSLAE}
AX^{T}_{i,\ast} = B^{T}_{i,\ast}, \quad i=1,\ldots,m.
\end{equation}
В \eqref{imgSLAE} $X_{i,\ast}$ и $B_{i,\ast}$~--- строки исходного
и зарегистрированного изображений, $h=(h_j)$~--- вектор аппаратной
функции, $l$~--- его длина, $m$ и $n$~--- размеры изображения.

%Вектор аппаратной функции должен быть нормирован по $\| \cdot \|_1$-норме: $\sum_{j=1}^l = 1$ \cite{Sizikov}.

Для решения получившейся СЛАУ \eqref{imgSLAE} может быть применен
РМНК, а также другие методы~[16].
%\cite{Sizikov}.


\textbf{4. Вычислительные эксперименты.} На рис.~1--3 представлены
результаты вычислительных экспериментов, в ходе которых описанный
выше метод, реализованный с использованием системы Matlab,
применялся для решения задачи восстановления изображения,
зарегистрированного с помощью устройства с неточно заданной
аппаратной функцией.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%\begin{figure}%\label{fig1}
%\begin{center}

%\begin{tabular*}{0.6\textwidth}{l @{\extracolsep{\fill}} c @{\extracolsep{\fill}} r}
% {\small а}  & {\small б} & {\small в}  \\
%\end{tabular*}

%\vspace{-4ex}

%\includegraphics[width = \textwidth]{fig1}

%\begin{subfigure}{0.3\textwidth}
%{\small а}
%\includegraphics[width = \textwidth]{fig1_1}
%\end{subfigure}
%\begin{subfigure}{0.3\textwidth}
%{\small а}
%\includegraphics[width = \textwidth]{fig1_2}
%\end{subfigure}
%\begin{subfigure}{0.3\textwidth}
%{\small а}
%\includegraphics[width = \textwidth]{fig1_3}
%\end{subfigure}

%\vspace{-5ex}

\begin{figure}[h!]
\centering{
\includegraphics[scale=1]{01/fig1}

\vskip 2mm {\small {\it Рис. 1.} Исходное, искаженное и
восстановленное изображения} \\

\vskip 0mm {\footnotesize Объяснение в тексте.}\vskip 5mm
%\end{center}
%\end{figure}
%
%
%\begin{figure}\label{fig2}
%\begin{center}
%\includegraphics[width = 0.7\textwidth]{fig2}
%\begin{figure}[h!]
%\centering{
\includegraphics[scale=1]{01/fig2}

\vskip 2mm {\small {\it Рис. 2.} Зависимость относительной
погрешности решения $\varepsilon$\\ от параметра
регуляризации~$\alpha$} }
%\end{center}
\end{figure}


%\begin{figure}\label{fig3}
%\begin{center}
%\includegraphics[width = 0.7\textwidth]{fig3}
\begin{figure}[h!]
\centering{
\includegraphics[scale=1]{01/fig3}

\vskip 2mm {\small {\it Рис. 3.} Числа обусловленности матриц
$A_E$ ({\it 1}) и $A_{\omega}$ ({\it 2})\\ при различных значениях
параметра~$\alpha$}
%\end{center}
\end{figure}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


Цель указанных экспериментов~--- эмпирическое определение значений
параметра регуляризации $\alpha$, при которых задача
восстановления изображения может быть решена с наименьшей
относительной погрешностью, а также тестирование предложенного в
п.~2 алгоритма и проверка теорем~5 и 6.

В вычислительных экспериментах моделировалась регистрация
изображения технической системой, на аппаратную функцию которой
наложен случайный шум, распределенный по нормальному закону с
нулевым средним и стандартным отклонением 0.01. Для получения
<<шума>> применялась встроенная функция imnoise системы Matlab.

С использованием этой <<зашумленной>> аппаратной функции
моделировалось получение <<зарегистрированного с искажениями>>
изображения, которое затем восстанавливалось построчно с помощью
регуляризованного метода наименьших квадратов А.~Н.~Тихонова
(вместо <<зашумленной>> бралась гипотетическая <<точная>>
аппаратная функция).

В эксперименте использовалось модельное изображение черного кольца
на белом фоне.

Размер изображения~--- $50\times50$ пикселей.

На рис.~1 представлены исходное изображение ({\it а}),
изображение, <<зарегистрированное с искажениями>> ({\it б}), а
также наилучший из результатов восстановления изображения ({\it
в}).



Наилучшее восстановление неточно зафиксированного изображения
удалось осуществить при $\alpha^{\ast} = -0.1671$. Относительная
погрешность решения при указанном значении параметра регуляризации
составила $\varepsilon = 0.1307$.

На рис.~2 показана зависимость относительной погрешности
восстановления изображения $\varepsilon$ от параметра
регуляризации $\alpha$.




На рис.~3 приведены значения чисел обусловленности матрицы $A_E$
системы \eqref{sist_alpha} и матрицы $A_{\omega}$ (\eqref{sZH} и
\eqref{sZHm}). Видно, что матрица $A_{\omega}$ имеет меньшее число
обусловленности как для положительных, так и для отрицательных
$\alpha$.




\textbf{Заключение.} В работе рассмотрен ряд важных теоретических
и практических аспектов регуляризованного метода наименьших
квадратов А.~Н.~Тихонова, одним из которых является методика
построения модельных СЛАУ с точной правой частью и приближенной
матрицей, позволяющая получить априорные нижние оценки
максимальной относительной погрешности метода РМНК.

Показано, что при определенных условиях РМНК сводится к
минимизации сглаживающего функционала, методу наименьших квадратов
и, что является неожиданным результатом, к поиску стационарной
точки сглаживающего функционала с отрицательным параметром
регуляризации.

Построено и обосновано алгебраическое преобразование, снижающее
обусловленность численного решения задачи РМНК, наиболее
актуальное при использовании отрицательного параметра
регуляризации. Приведен иллюстративный пример, характеризующий
вычислительные аспекты РМНК~--- прикладная задача восстановления
изображения, зарегистрированного прибором с неточно заданной
аппаратной функцией, представлены результаты соответствующих
вычислительных экспериментов.\\

Авторы благодарят участников семинара СПбГУ по конструктивному
негладкому анализу и недифференцируемой оптимизации (CNSA\&NDO) и
его руководителя профессора Василия Николаевича Малоземова за
конструктивное обсуждение результатов статьи, ценные замечания и
пожелания.




%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\input{01/lit-ra}

%%%%%N DOI в~ссылке!!!!!!!!!!

\input{01/ref-s}

%%%%%N DOI в~ссылке!!!!!!!!!!

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


{\footnotesize



\vskip 3mm

%\thispagestyle{empty}



\thispagestyle{empty} %очищаем стиль страницы
\thispagestyle{fancy} %включаем пользовательский стиль
\renewcommand{\headrulewidth}{0pt}%
\fancyhead[LO]{}%
\fancyhead[RE]{}%
\fancyfoot[LO]{\footnotesize{\rm{Вестник~СПбГУ.~Прикладная~математика.~Информатика...~\issueyear.~Т.~13.~Вып.~\issuenum}}
\hfill}%
\fancyfoot[RE]{\hfill\footnotesize{\rm{Вестник~СПбГУ.~Прикладная~математика.~Информатика...~\issueyear.~Т.~13.~Вып.~\issuenum}}}%
%\lhead{} %верхний колонтитул слева
%%\rhead{} % верхний колонтитул справа
% для оформления нижнего колонтитула
\cfoot{} %
%\lfoot{} %
%\rfoot{\thepage} %


%\noindent Статья рекомендована к~печати проф. Л. А. Петросяном.

%\vskip 1mm

\noindent Статья поступила в~редакцию 2~июня~2016~г.

\vskip 1mm

\noindent Статья принята к~печати 19~января~2017~г.


}


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%\vskip 5mm

%{\footnotesize

%\noindent К\,о\,н\,т\,а\,к\,т\,н\,а\,я\,
%и\,н\,ф\,о\,р\,м\,а\,ц\,и\,я \nopagebreak \vskip 3mm

%{\it Балонин Николай Алексеевич}~-- доктор технических наук,
%профессор; e-mail: korbendfs@mail.ru

%{\it Сергеев Михаил Борисович}~-- доктор технических наук,
%профессор,  директор; e-mail: mbse@mail.ru

%\vskip 2mm

%\emph{Balonin Nikolaj Alekseevich}~-- doctor of technical
%sciences, professor; e-mail: korbendfs@mail.ru


%\emph{Sergeev Mikhail Borisovich}~-- doctor of technical sciences,
%director; e-mail: mbse@mail.ru


%}


%\thispagestyle{empty}
